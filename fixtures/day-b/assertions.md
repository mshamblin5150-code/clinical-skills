# day-b — assertion set

Twelve encounters from a single walk-in shift, 2025. Visit date and site removed per [fixtures/README](../README.md). Skill: `clinical-note`, comprehensive SOAP branch.

This set exists for one thing day-a cannot test: **what the skill does with a vital it had to invent.** Every day-a case carries a complete vital line, so nothing there exercises the filled half of the license. Nine of these twelve carry no vital at all.

**It now carries the OLDCARTS half of the same license.** [Issue #30](https://github.com/mshamblin5150-code/clinical-skills/issues/30) admitted a third member to the filled class — the HPI severity — and it was found on **case 9 of this set**, whose run wrote `Aggravating - not documented ... Severity - not documented`. B5 and B6 are that half, and they reach **all twelve** rather than B1's nine — a case can carry a full vital line and still leave seven OLDCARTS boxes empty, so the two splits are independent. B7 and B8 then divide the twelve on where the severity comes from, which is a third split again: seven cases write a score, two write the absence of one, three write neither.

**And it now carries the boundary that split left open.** B8 took two of those three and deliberately left case 3 out, because her complaint is itching and no ruling said what a scale of pain reads for a patient who does not hurt. [Issue #42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) made that ruling on 2026-08-13 — the scale scores **pain** whatever the complaint is, and the words after the number name the complaint rather than what is scored — and **B14 is the third of those three**, on the reasoning under its own heading. It arrives from a question rather than from a defect, which B9's and B10's tickets did too — what is unusual is that the answer **corrected** the sentence the skill had been carrying rather than confirming it.

**And it now carries the direction all of those rows point the wrong way down.** B1 through B3 and B5 through B8 ask what work a generated value made the note do — B4 is the odd one out, guarding givens. [Issue #27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) was a generated value making the note do **less** — case 9's filled `T 98.8 F` and `SpO2 97%` deferring a chest film for a documented `lung sounds diminished`, which the clinician read and agreed with. B9 is that direction, and D7 is the lung finding it turns on, which had no row until now.

**And it now carries the social and allergy half of the same license.** [Issue #29](https://github.com/mshamblin5150-code/clinical-skills/issues/29) ruled that the slots the branch templates enumerate are boxes like the OLDCARTS eight — a value is required and the shorthand constrains none — and that the hedge the fixtures were rewarding, `not documented this visit`, is a sentence **drift row 12 had already banned**. R2, R3 and R4 are that ruling, and **R1 is the row it reversed most of.** The reversal was not free: it struck two of R1's three claims and left the third standing on a different argument, which is written out under the row.

Opened for [issue #8](https://github.com/mshamblin5150-code/clinical-skills/issues/8).

## Status — both halves built

**The inputs are in.** All twelve encounters are in [shorthand/](shorthand/), one file per case, de-identified.

**The reference is read.** All twelve submitted notes were opened in the portal on 2026-08-11 and are kept, un-de-identified, in `scratch/day-b-reference/` — gitignored, because they carry the visit date, the site, a named outside physician and the patient references. Every row below now records what the submitted note actually did.

**Inputs must come from the day file, never from the generated notes**, and the same trap applies to the reference half. That is why the rule below stood:

> ~~Until it is, no drift row may be added to this set.~~ **Lifted 2026-08-11.** The reference is read, so a drift row can now be checked against what the submitted note did rather than against the skill's own prior output.

The rule was day-a's, and it was worth keeping: four of day-a's six DRIFT rows changed when its reference was finally read, and two had claimed the clinician abandoned a finding he had in fact carried into the Assessment and the Plan.

**Here it changed one of five.** D1 through D4 turn out to say what a careful read of the input would have said. D5 is the exception, and in the direction the rule exists to catch: the submitted note **addressed** the low magnesium, so a row asserting it was abandoned would have been false. Its `Reference did` cell reads *neither*, not *better*, and it is the set's only anti-regression row.

**D6 arrived after that count and is not in it.** It comes from [#32](https://github.com/mshamblin5150-code/clinical-skills/issues/32) — a `clinical-note` run over case 9, not the reference read — and the read is what lets it carry a verdict at all.

**D7 arrived after it too, from [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27), and is not in it either.** Unlike D6 it was written with no verdict, because the read had already happened; [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43)'s re-read has since supplied one and the reference **fails** it. So *"here it changed one of five"* is a statement about D1 through D5 and stays one — the re-read added a sixth drift failure rather than revising any of the five.

**Run 3, 2026-08-12, on commit `b65b9f6`: `DRIFT 7/7` · `FILLED 10/11` · `CODING 2/2` · `REPORTED 1/4`. The run fails.** **`REPORTED` read `2/4` until 2026-08-14**, when [#95](https://github.com/mshamblin5150-code/clinical-skills/issues/95) settled R4's reading and the row went to a fail; the run failed before that ruling and fails after it, on B6. **Twenty-four of the thirty-one rows scored** — every row that existed on that commit, and the widest pass this set has had. FILLED is binary and **B6 misses on case 8**. Output in `scratch/day-b-run-3/`, scorecard and reasoning in `SUMMARY.md` there. [#75](https://github.com/mshamblin5150-code/clinical-skills/issues/75).

**Seven rows are unscored. Four landed while run 3 was in flight and three landed after it.** G1 came from [#66](https://github.com/mshamblin5150-code/clinical-skills/issues/66) with the `GIVEN` class; **B12, B13 and R5** came from [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67), which also moved `skills/clinical-note/SKILL.md`. **B14** came from [#42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) on 2026-08-13, after the run rather than during it. **B15 and B16** came from [#47](https://github.com/mshamblin5150-code/clinical-skills/issues/47) on 2026-08-15, from **this run's own case 7**, and moved that same file again. All seven are ungraded on this file's standing rule: **the rows exist because the skill changed, and these twelve notes predate the change**, so a score from them would belong to neither commit. That is the refusal runs 1 and 2 both made, arriving a third through seventh time.

**The denominator moved three times inside one ticket.** #75 asked for **twenty-two** rows — run 2's nineteen plus #29's three, omitting B10 and B11. The set held **twenty-four** when the run started, and both extra rows were run anyway. #66 made it **twenty-five** while the notes were being written, and #67 made it **twenty-eight** before the record could be merged. **Nothing here went wrong and the number moved three times**, which is [fixtures/README](../README.md)'s pattern at its sharpest: rows are cheap to write and a run costs twelve generations plus seven graders.

**Run 2, 2026-08-12, on commit `e4cd503`: `DRIFT 7/7` · `FILLED 9/9` · `CODING 2/2` · `REPORTED 0/1`. The run passes.** Every row that existed on that commit was scored — **nineteen, and the set now holds thirty-one.** Output in `scratch/day-b-run-2/`, scorecard and reasoning in `SUMMARY.md` there. [#55](https://github.com/mshamblin5150-code/clinical-skills/issues/55).

**Ten rows landed after that run and five tickets put them there.** B10 and B11 come from [#33](https://github.com/mshamblin5150-code/clinical-skills/issues/33); B10 was scored by [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43)'s re-read and B11 never can be. R2, R3 and R4 come from [#29](https://github.com/mshamblin5150-code/clinical-skills/issues/29), **which branched from `e4cd503` — run 2's own base** — and which also rewrote R1. **B14 is the tenth and the latest**, from [#42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) on 2026-08-13, and it landed the same day as #43's re-read with neither branch able to see the other. So this scorecard is complete for its commit, and **`REPORTED 0/1` grades a row whose text has since changed.** See *Run 2 was scored against the previous R1* below before reading that digit as current.

**G1 is the sixth and it brings a class with it**, from [#66](https://github.com/mshamblin5150-code/clinical-skills/issues/66) — the first `GIVEN` row anywhere, written from a defect run 2 committed and no row then existing could catch. **`GIVEN n/n` therefore has no value at all**, which is a different thing from a class with no rows and the same position `CODING` was in after run 1. It is the one row here whose behavior is already known: see its own section for why a known failure is still recorded as unscored.

**B12, B13 and R5 are the last three, from [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67), and they are written from a defect run 2 committed nine times over.** They score *which value was chosen* — the half of the filled-vitals license B1 and B3 leave open. **B13 is the first row in this repo whose `Cases` column reads *the set*** rather than a case list, because its subject is two notes agreeing and no per-case row can see that. They carry no verdict for the reason the six above them do not, with one difference worth stating: **run 2's output is what the rows were written from, so their failure is known before they are scored** — G1's position exactly, and recorded the same way.

**Nine rows have a value for the first time** — D6, D7, B5 through B9, C1 and C2 — and **run 1's one binary miss is closed**: case 5's filled BMI is now named in the Assessment and the Plan.

**Run 1's other miss is not closed.** It scored `REPORTED 0/1` and so does run 2; R1 is counted rather than enforced, which is the only reason the run passes with it open. **Which assertions made it a miss changed under #29**, and two of the findings recorded against run 2 are reversed there — the digit survives for different reasons than it was awarded for.

**Run 2 also beat the reference on B4**, which is a different thing from closing a run 1 miss and is worth not conflating: the submitted note recorded the given `ht 6'2"` as `6'1" (73 in)` and derived a BMI from the altered value. Run 2 carries 74 inches and derives from it. Run 1 passed B4 too — the row's failure was always the reference's.

**A pass is a smaller claim than it looks, and three things below say why**: [#47](https://github.com/mshamblin5150-code/clinical-skills/issues/47)'s defect recurred on a case B3 still passed, B2 cleared on the same exit again, and R1 went backwards. Read those before reading the scorecard as a bar cleared.

### Run 3 is the first full pass, and it fails on one row

**Run 3, 2026-08-12, on `b65b9f6`, over the twenty-four rows then existing.** Twelve generating passes with each shorthand pasted inline, seven grading passes split by class that had not seen the generation, and an orchestrating pass that authored no note and re-derived every row from the output text. All twelve generating passes reported opening nothing under `fixtures/`.

**The miss is B6, on case 8.** Its block reads `OLDCARTS Character, Timing filled, and the second half of Aggravating, "movement and activity worsen the body aches", filled` — **Character and Timing are named without their values**, and neither value appears anywhere in the tier block. That is the row's fail condition word for word: *the block names the field without its value*. The HPI itself is complete, which is what makes the miss the one B6 exists to catch — a reader of that note cannot tell which of the eight elements were invented. The other eleven cases declare every filled element with its value.

**The matrix could not have caught it, and did not claim to.** Case 8's row 17 counts the eleven `SH:` clauses and the allergy slot and says nothing about OLDCARTS, so this is a gap in the self-report rather than a false report by it. That is a different failure from the three below and worth keeping separate.

**B5 came through clean on all twelve**: all eight elements carry a value on every case and every severity is a number. So the OLDCARTS half of the completeness question is answered and the social half is not — two rows with nearly the same shape, landing opposite ways on the same twelve notes.

**#75 predicted R2 would be the row most likely to fail on a first scoring, and it was right.** The ticket's reasoning was that run 2's notes hedged and nobody had measured whether the current skill still does. It does, on two of twelve, and in the one place the rule leaves no alternative — see below. A prediction that lands is worth recording as one, because the next set to be scored for slot completeness now has a prior.

**B10 and B11 have a value for the first time, and drift row 16 has now been exercised against a note.** Both pass. Case 8 writes `2 days for the headache, fever, cough, sore throat and weakness, 1 day for the right earache`; case 9 writes `2 days for congestion, sneezing and cough, 1 day for the facial pain, worsening`. **Neither raises a FLAG**, which is the whole of what [#33](https://github.com/mshamblin5150-code/clinical-skills/issues/33) asked for — run 1 carried both durations and flagged them as unreconciled.

**R1 fails on one slot on one case, and the slot list is the finding rather than the digit.** #75 asked for exactly this, because the digit has been stable across three runs for three different reasons:

| Run | Case 1 failed on | Case 2 failed on |
| --- | --- | --- |
| 1 | occupation, education | occupation, education |
| 2 | education, diet, sleep | education, diet, exercise, sleep |
| **3** | **education** | **nothing** |

**All three rows are read under the narrowed R1**, which is not how runs 1 and 2 were scored at the time. Both were graded against text [#29](https://github.com/mshamblin5150-code/clinical-skills/issues/29) later replaced, and their allergy and marital-status findings are struck above. The table is a like-for-like comparison built by re-reading the earlier findings under today's row, not a transcript of three scorecards — which is the only way three runs under two different texts can be put in one column at all.

Every lifestyle slot has moved to the least it can say — `no dietary restriction reported`, `no regular exercise program reported`, `no sleep disturbance reported` — and `Retired` on the 71-year-old is grounded by age, as #29 ruled. What is left is case 1's `high school graduate`, whose own FILLED line is bare where every neighbor cites its ground: `SH education High school graduate filled` against `SH occupation Employed filled, grounded in "while he was at work"`.

**R2 fails, and it fails somewhere the rule has no passing form.** Every `SH:` line on all twelve is clean — eleven of eleven slots, no hedge, no blank. The failure is entirely in the allergy slot, on the two cases whose shorthand supplies **given allergens with no documented reaction**: case 7 writes `reaction not documented` six times and case 11 writes `specific reaction not documented`. That is the string #29 banned by name.

**And there is nothing else those notes could have written.** `NKDA` is false, a stated allergen may not be dropped, and the reaction is genuinely unknown — #29 blessed case 2's `Seasonal-itching/sneezing` because itching and sneezing are what seasonal allergies do, and **nothing grounds a reaction to norfloxacin.** So the box demands a value, the grounding rule supplies none and row 12 forbids the hedge. **The rule is complete for a silent allergy slot and incomplete for a given allergen**, and two of twelve inputs hit the gap. Listed under *Still unresolved*.

**R4 fired on five of nine, and four cases are vacuous.** Cases 7, 10, 11 and 12 carry a **given** allergy status, so no inferred value could underwrite anything there; three of the four say so in the block rather than leaving it silent. On the five that fired, cases 6 and 8 are the row working properly — they draw the given-versus-proposed line drug by drug and name only what the note added. **Cases 5 and 9 enumerate the wrong drugs**: each names an antibiotic the shorthand's own plan already ordered and omits the acetaminophen the note added. **Scored a fail**, on the reading [#95](https://github.com/mshamblin5150-code/clinical-skills/issues/95) settled on 2026-08-14: the row asks that the line enumerate correctly, not merely exist, so naming a given fails and omitting the proposed acetaminophen fails, and cases 5 and 9 do both. **This was recorded as a pass until that ruling** — each line does disclose the dependency out loud, which is the harm the rule names, and read that way the two cleared the bar imprecisely rather than missing it. The ruling is what makes the row enforceable: a verdict that moved with the reader's strictness is the one thing [fixtures/README](../README.md) says a binary row may not have.

**[#47](https://github.com/mshamblin5150-code/clinical-skills/issues/47) recurred a third time, on a third case.** Case 7's drift matrix row 4 says the BMI of 28.3 *"is addressed with counseling and confirmed measurements at follow-up"*, and **that Plan carries no weight, nutrition or activity line at all** — the counseling lives in the Assessment and in `FILLED·proposed` and never reaches the Plan. Run 1 case 5, run 2 case 10, run 3 case 7. B3 survives on case 7 exactly as it survived on case 10, because the row is a disjunction and the Assessment names the value. **Nothing about #47 survives.** It was **not** resolved by editing a row: the clinician ruled the mechanism on 2026-08-15, `clinical-note` gained a narrowed drift row 4 and a new **drift row 21**, and this set gained **B15 and B16** — B3 is untouched and still passes case 7.

**Three matrices reported row 17 as a pass on the three cases that fail R1 and R2.** Case 7's says *"twelve carry a value and none is a hedge"* while its own allergy line hedges six times; case 11's says *"No 'not documented' … appears in any of them"* while its allergy line and its GAPS block both say it; case 1's says the education slot *"takes the least its slot can say"* while it reads `high school graduate`. That is [ADR 0001](../../docs/adr/0001-fixture-asserts-on-named-findings.md)'s argument happening for a fourth time, on the one row these assertions independently check.

**And it happened to run 3 as well, which makes it four consecutive runs.** [#66](https://github.com/mshamblin5150-code/clinical-skills/issues/66) landed on `main` as `5af7624` while run 3 was in flight, moving `skills/clinical-note/SKILL.md` **and adding G1 with the `GIVEN` class**. So this is the ordinary case a fourth time: the row exists because the skill changed, the twelve notes predate the change, and a score from them would belong to neither commit. **G1 is unscored and the denominator says so.**

**Run 3's case 12 is evidence about #66 and is not a score on it — including on G1.** The defect #66 was raised on is run 2's case 12, which dropped the given `spot mono` and wrote `No monospot sent on those findings`. **Run 3 carried it through** — the order is in the Labs line, the mononucleosis entry reasons from it as pending, the Education says she will be called with the result, and the tier block states *"The orders themselves are given."* That establishes the defect is not unconditional, exactly as run 2's case 9 did for B9. **It does not score G1**, and reading it as `GIVEN 1/1` would be the mistake this file has refused three times — the notes were written before the rule the row holds them to.

**What run 3 does not establish is that the skill changed, and the three failures are three different kinds of thing.** **B6 is a regression** — run 2 scored it and passed it, and run 3 loses it on one case of twelve. **R1 is a re-score** — it had a value under text #29 replaced, and fails again under the new text on a shorter list of slots. **R2 had never been scored at all.** Only R2 is the growing-denominator story this file has told three times; only B6 is a row going backwards. Twelve notes are still one run, and one case of twelve is not a property of the skill.

### Run 1, and what run 2 is measured against

**Run 1, 2026-08-11, on commit `d213e35`: `DRIFT 5/5` · `FILLED 3/4` · `REPORTED 0/1`. The run fails.** Output in `scratch/day-b-run-1/`, scorecard and reasoning in `SUMMARY.md` there.

FILLED is binary, and **B3 missed on case 5**: a filled BMI of 28.4 — the overweight band, so the row fires — reached the Objective, the Medatrax field and the FILLED block and was named in neither the Assessment nor the Plan.

**Of B3's nine cases, seven produced an abnormal filled BMI and six of those addressed it.** Cases 6 and 12 landed normal, at 23.0 and 21.6, so the row never fired on them. Case 5 is the only case where it fired and was missed.

**The note's own drift matrix reported row 4 as PASS**, on the stated ground that the BMI was addressed through weight counseling in the Plan. That Plan contains no weight counseling. [ADR 0001](../../docs/adr/0001-fixture-asserts-on-named-findings.md) argues a self-report cannot grade a run because the run that misses a finding is the run that reports PASS; this is that argument happening rather than being made.

**Unlike day-a's run 1, this one was not graded by the pass that produced it.** Six subagents wrote the notes without access to this file, a fresh pass graded the output text against it, and the orchestrating pass re-derived every row from the output files having authored none of them. What that leaves is narrower than what it removes: one session directed both halves, so a framing shared between them is not excluded.

**Run 2 was run the same way and cost one thing less.** Twelve generating passes, one per case, each given its shorthand **pasted inline rather than a path into `fixtures/`**; five fresh grading passes split by class, none of which had seen the generation; and the orchestrating pass re-deriving every row from the output text having authored no note. Every generating pass was audited afterwards and **all twelve reported opening nothing under `fixtures/`**, each naming this repo's own `AGENTS.md` link to [fixtures/README](../README.md) as one it deliberately did not follow. Run 1 regenerated five cases on that — five of the seventeen across both sets run that day, this set's twelve and peds-bp's five; run 2 regenerated none. The audit is still a self-report and still cannot be made anything better — what changed is that the file was withheld up front rather than trusted to be skipped.

### Run 1 covered ten of nineteen rows, run 2 covered nineteen of thirty-one, and both gaps have the same cause

**D6, B5–B8, C1 and C2 landed on `main` while run 1 was in flight** — [#32](https://github.com/mshamblin5150-code/clinical-skills/issues/32), [#30](https://github.com/mshamblin5150-code/clinical-skills/issues/30) and [#19](https://github.com/mshamblin5150-code/clinical-skills/issues/19), the same day, one of them arriving between the run's commit and its merge. The run was made against `d213e35`, where the set held D1–D5, B1–B4 and R1, and it graded every one of them. **So `DRIFT 5/5`, `FILLED 3/4` and `REPORTED 0/1` are complete for the commit they name, `CODING n/n` has no value at all, and the scorecard is not a full pass over the set as it now stands.**

**The new rows were deliberately not graded from run 1's output, and the reason is not laziness.** The output exists and the new rows are claims about note text, so scoring them would have been cheap. But B5 through B8 exist *because* `clinical-note` changed — #30 admitted the HPI severity to the filled class, #32 and #19 moved the same file — and those twelve notes were written before those changes. A score from them would measure neither the commit the notes came from nor the commit the rows came from. **A number that belongs to no commit is worse than a gap that names itself.**

**D7 and B9 landed after run 1 rather than during it**, from [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27), and the same reasoning holds with one wrinkle worth stating. They are ungraded here on the argument above: `clinical-note` gained drift row 15 with them, and those twelve notes were written before it.

**B10 and B11 landed later still**, from [#33](https://github.com/mshamblin5150-code/clinical-skills/issues/33), and are ungraded for the same reason a third time — `clinical-note` gained **drift row 16** with them. Run 1's case 9 output is again *evidence* rather than a score, and reading it as `FILLED 3/6` would be the mistake the paragraph above refuses. What it is evidence of is sharper than the ticket claimed, and it cuts both ways — see *B10 and B11 are the two limbs of one rule* below.

**But run 1's output is what this ticket's evidence is drawn from, and evidence is not a verdict.** Case 9's note is quoted under *B9 is the subtractive half of the license* below, because it shows a run reaching for filled values to defer a chest film — the second such run, after the one #27 reports. That establishes the defect is real and recurring. **It does not score B9**, and reading it as `FILLED 3/5` would be the exact mistake the paragraph above refuses. What would settle both rows is run 2.

What this asked for is run 2, against current `main`, over all nineteen rows. That is [fixtures/README](../README.md)'s own instruction — *re-run after every `SKILL.md` edit* — arriving immediately, and it is [#55](https://github.com/mshamblin5150-code/clinical-skills/issues/55). **It ran on 2026-08-12 and the nine gaps are closed**; the scorecard is at the top of this section.

**Run 2 settled B9, and it settled it the way the row hoped rather than the way the evidence predicted.** The paragraph above records two independent runs deferring case 9's chest film on filled numbers. Run 2 makes it two of three: the film is **ordered**, in the differential and in the Plan, and the note's own `FILLED·proposed` line says why — *"Ordered from the givens alone; no filled vital was allowed to make it unnecessary."* The filled `T 99.1 F`, `SpO2 96%` and `RR 18` are declared with their values and appear in no sentence about imaging. That is drift row 15 doing the work it was added for, on the case it was added from.

**One run is not a fix here either**, and the symmetry is worth keeping: the same sentence that refuses to read run 1's case 9 as evidence of a settled defect refuses to read run 2's as evidence of a settled repair. What run 2 establishes is that the defect is not unconditional.

### And then it happened to run 2 five times, four times from later commits and once from its own

**[#33](https://github.com/mshamblin5150-code/clinical-skills/issues/33) added B10 and B11** and gave `clinical-note` drift row 16. That is the ordinary case this section has now described three times: rows exist *because* the skill changed, the twelve notes predate the change, and a score from them would belong to neither commit. **This is the third consecutive run to be overtaken by a row**, which is [fixtures/README](../README.md)'s pattern rather than anyone's oversight.

**[#29](https://github.com/mshamblin5150-code/clinical-skills/issues/29) is the other kind, and it is worse.** It added R2, R3 and R4 and rewrote R1 **against `e4cd503` — the commit run 2 names.** The two were in flight against the same parent within hours and neither could see the other. So run 2 scored nineteen and its `REPORTED 0/1` was scored against **R1's previous text**.

**[#66](https://github.com/mshamblin5150-code/clinical-skills/issues/66) is the third and it is the ordinary kind again**, landing well after the run and bringing G1 and the `GIVEN` class. It is the first of the four whose row was written **from this run's own output** rather than from a ticket about something else — see the `GIVEN` section, which is also where the reason it is still unscored is argued rather than assumed.

**[#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67) is the fourth and it is #66's kind**, landing later still and bringing B12, B13 and R5, which took the set to twenty-eight rows against run 2's nineteen — and [#42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) is the fifth, adding B14 on 2026-08-13 for twenty-nine. **Two of the four are now rows this run's own output wrote**, which is a change in where rows come from and not just a change in how many there are: the first two arrived from tickets about other things, and the last two arrived because someone read twelve outputs side by side. A run that passes every row is still the best source of the next ones.

**A row that was never scored is a gap that names itself; a row scored against a superseded claim does not.** D7, B9, B5–B8, B10 and B11 all show up as a denominator a reader knows to discount. R1 *was* scored, against a claim the same base commit was in the act of replacing, so the number sits in the scorecard attached to a row that no longer says what it said. **It reads exactly like a current score**, and the `Last run` column cannot show the difference on its own — which is why it is written out here.

**Run 2's output is still evidence, and unusually good evidence.** That is run 1's case 9 argument reused: the notes are real output of the pre-#29 skill, and what they did with the social slots is a fact whatever row was pointed at them. **They falsified part of the narrowed R1 as first written** — see the row's own section, where *occupation and education* had to become the fuller list run 2 actually produced, and where `Retired` on a 71-year-old turned out to be grounded rather than invented. Evidence is not a verdict, and that distinction has now paid twice in this file.

**What it does not do is score R1, R2, R3 or R4.** Those notes were written before drift row 17 existed, before the hedge was banned and before the slot rules were assigned. **The re-run is [#75](https://github.com/mshamblin5150-code/clinical-skills/issues/75)**, and unlike every run before it, it has a row to *re-score* as well as rows to score for the first time. **It ran on 2026-08-12 and scored `REPORTED 1/4`** — R1 re-scored and still failing, on a shorter list of slots than either run before it, and R2, R3 and R4 scored for the first time. **That digit read `2/4` until 2026-08-14**, when [#95](https://github.com/mshamblin5150-code/clinical-skills/issues/95) settled R4's reading and took it to a fail: the run has now had a row re-scored twice over, once by a re-run and once by a ruling. The section above is that run.

## The reference is a baseline, not a target

Same four verdicts as [day-a](../day-a/assertions.md), and only *worse* is a regression. The `Reference did` columns below say which:

- **Better** — the skill caught something the submitted note dropped.
- **Worse** — the skill lost something the submitted note had. The most important thing this set can find.
- **Neither** — different wording for the same content.
- **Out of reach** — the submitted note is better on information the skill never had. The clinician was in the room; the skill has the shorthand and nothing else. Matching this is not a target and failing it is not a regression — **the skill is required not to try.**

**The fourth class arrived while this set could not host it.** It was added to day-a on 2026-08-11, and the commit that added it recorded why day-b was left on three: *"neither has read its reference, so neither can host the class yet, and both belong to other tickets."* This is that other ticket. day-b's one row of the class is **R1**, below.

**Reading it moved this set in both directions.** It supplied five drift rows the set was forbidden to carry, and it also **failed two of the four FILLED rows the set held then** — B4 on case 4 and B2 on case 8. (B5 through B9 and D7 arrived after the read and carried no verdict from it. **A second read on 2026-08-13 settled B5, B7, B8 and D7 of those, and B10, B13, R2, C1 and C2 besides** — see *The reference was re-opened for the HPI*. B6 and B9 are the two it could not settle, and no read can.) A bar the reference clears everywhere is a bar set too low; a bar it fails is where the skill has something to be better than.

### The provenance question was asked, and it has no answer

A submitted note does not say whether a value was measured at the visit or supplied at write-up, and these were typed 78 days after the shift. **The clinician was asked directly, 2026-08-11, and could not answer**: *"i probably made them up… i can't give you a real answer."*

`reference/medatrax-fields.md` points the same way — every 2025 Fall and 2026 Spring encounter fills Height and BMI, *"inventing a height where the shorthand carries none"*, in his words *"the newer records everything is filled out."* That is a pattern, not a record of what happened at these nine bedsides, and no read of the portal will ever produce one.

**So no row here may depend on the answer, and none does.** Every `Reference did` cell below is worded to hold whether the value was measured or invented:

- **B1** asks only that something went in every box. True either way.
- **B3** asks what happened to a value *downstream* — Objective only, or named in the Assessment. True of the value whatever its origin.
- **B4** is about **given** vitals, where the shorthand supplies the value and the comparison is direct.
- **B2 used to depend on it**, and was rewritten so it no longer does. See below.

### B2 was wrong, and the clinician is what corrected it

The row previously read: a hypertensive with no recorded pressure must not be given a *normal* one, on the rationale that hypertension makes a normal pressure implausible. Asked about case 8's 124/78, he answered: **"hell she may be compliant with her BP meds."**

He is right, and it breaks the rationale. A treated, compliant hypertensive at 124/78 is not an implausible value — **it is the goal of the treatment.** A row demanding an abnormal pressure from every documented hypertensive was demanding an invented abnormal finding, which is the thing standing rule 2 exists to forbid. The set had it backwards.

**B2 now has two exits and forbids only silence.** A filled pressure for a documented hypertensive may be abnormal, or it may be normal *and* the note says the hypertension is controlled or treated. What fails is a normal number with no account of why it is normal — because that is the run that picked 124/78 out of the air, and it is indistinguishable from the run that reasoned unless the note says so.

**Case 2 is the in-corpus proof the passing form is writable.** A *given* 121/61 with the same `htn` history, and the Assessment reads `HTN, controlled (I10)`. That is the shape B2 now asks for. `test_corpus_census.py::test_case_2_is_the_normal_hypertensive_the_rule_rests_on` pins that reading by value, so an edit to the shorthand voids the claim loudly.

**The skill file has since been brought into line, and the corpus is what settled it.** For a while this row and [SKILL.md](../../skills/clinical-note/SKILL.md) disagreed on purpose — B2 was rewritten and the clinical rule was left alone, because changing one is the clinician's call. He ruled on 2026-08-11 ([#23](https://github.com/mshamblin5150-code/clinical-skills/issues/23)), and the count is what he ruled on: **175 of 551 encounters document hypertension, 96 transcribe a pressure, and 39 of those 96 are normal.** The retired rule would have contradicted 39 of his own readings. SKILL.md now carries B2's two exits as prose, the same four accepted forms of an account, and **drift row 14**, which is this row's rule stated where a run walks it while writing rather than only where a fixture scores it afterwards.

**The four ripple sites were checked rather than assumed**, which #23 asked for by name because a clinical rule does not change alone. `peds-bp` P2 and P3 needed nothing — P2's rationale was already rewritten on this ruling and P3 is a threshold row that never mentions a condition. `day-a` D3 and D10 needed nothing either, and for a reason row 14 now states outright: both are **given** pressures, and a given value cannot fail a row about filled ones. Drift row 4 is untouched and keeps its number. The one site that did need work was `fixtures/obesity-bmi`, which #23 did not list — its *Still unresolved* bullet had predicted this rewrite would reach O2, and it did.

**What did not change is the inferred regimen.** *"Infer the likely regimen"* survives untouched, and the ruling put it to work: an inferred antihypertensive in the Medications section **is** an account for a normal filled pressure. Two separately licensed fills agreeing is not the compounding standing rule 2 forbids, because the hypertension itself is a given.

## DRIFT — binary, all must pass

Each row is a finding the shorthand documented that a note can drop between the Objective and the Assessment. A run passes the row when the finding is named in the **Assessment or the Plan** — not merely recorded in the Objective.

**Six of the seven are findings the submitted note did drop. D5 is not** — see below the table. D7 arrived after the 2026-08-11 read and was scored by the 2026-08-13 re-read, [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43); it drops like the other five.

| # | Case | The finding | Fails when | Reference did |
| --- | --- | --- | --- | --- |
| D1 | 3 — 57 F | BP 147/81, with `htn` in the history | The elevated pressure is not named in the Assessment or the Plan. **A bare `BP` inside an age-based screening list is not a naming** — see below | Recorded it in the Objective and the portal field and stopped. The Assessment names scabies, traumatic otitis externa and tobacco use; **`hx htn` is dropped from the note entirely** — no I10, no recheck, no home log. Addressing it is *better*. |
| D2 | 1 — 36 M | Breath sounds diminished in all four fields, in a 1 PPD × 24-year smoker | Not named in the Assessment or the Plan | Carried it into the Objective and stopped. The Assessment codes `Tobacco use (Z72.0)` but never the finding; no spirometry, no film, no COPD consideration. Naming it is *better*. |
| D3 | 11 — 32 M | Inspiratory wheezing in all fields, in a documented asthmatic | Absent from the Assessment or the Plan | Recorded it on exam, then **recast it in the HPI as `inspiratory wheeze history`** — past tense — and carried asthma only in the *pre-existing* code list. No inhaler, no peak flow, no asthma plan item. Addressing it is *better*. |
| D4 | 7 — 67 F | Elevated liver enzymes — AST 48, ALP 136 — in a patient on a statin | The elevated enzymes are absent from the Assessment or the Plan | **Dropped them.** No AST, no ALP, no statin and no hepatic follow-up anywhere in the note, and a CMP ordered without saying why. The shorthand had already written the follow-up (`f/u pcp re elevated lft`). Keeping it is *better*. |
| D5 | 10 — 48 M | Magnesium 1.6, written in the shorthand as `labs good mg 1.6` | The low magnesium is absent from the Assessment or the Plan | **Caught it** — `Mg 1.6 → recommend OTC magnesium supplement`. Matching this is *neither*; losing it is *worse*. |
| D6 | 9 — 44 F | A documented positive COVID contact — `daughter inall was postive for covid` — alongside a congruent respiratory presentation | The Plan does not order COVID-19 testing and influenza testing, with a specimen named. Group A strep is required too, because this input documents the pharynx — `sore throat` and `pharyngeal erythema`. **Carrying the exposure in the HPI is not acting on it** — see below | **Ordered no testing at all.** The exposure reached the note — the HPI reads `COVID exposure in family` — and the Plan carries amoxicillin-clavulanate, a steroid dose pack, otic drops and an IM steroid, with no swab of any kind. Ordering it is *better*. |
| D7 | 9 — 44 F | `lung sounds diminished`, in a patient whose shorthand carries **no vital line at all** | Not named in the Assessment or the Plan | Carried it into the Objective and stopped. The exam reads `Lungs: diminished;`, the Assessment names acute bacterial sinusitis and acute otitis externa, and the Plan orders no film and no pulmonary item at all. **The third of three lung findings dropped the same way on one shift** — D2 on case 1, D3 on case 11, this. Naming it is *better*. |

**D1's fail condition is worded against a real decoy.** The submitted note's Plan contains the string `BP` — inside `Screenings: Colon CA (age), mammogram, BP, smoking cessation`, boilerplate that appears on other notes from this shift regardless of the pressure recorded and would read identically had the pressure been 118/70. A row that passed on a substring match for `BP` would score the reference as having addressed 147/81. It did not.

**D5 does not fit the sentence above the table, and it is kept anyway.** Nothing was abandoned: the shorthand's own plan line already reads `recommend mg otc mg supplement`, and the submitted note carried it through. So D5 is not a defect the reference committed — it is the one place on this shift where the reference did the right thing, and the row asks the skill not to fall behind it. day-a keeps D3 and D5 on the same terms, both marked *neither*.

**What it still tests is the decoy in the input.** `labs good mg 1.6` wraps a low magnesium inside a phrase that says the labs were fine. A run that reads `labs good` and moves on drops the abnormal, and the recommendation two words later is the only thing that would catch it. The row is checkable from the input alone; what the reference added was the verdict — *neither*, not *better*.

**Case 2 is the counter-example that makes D1 legible.** The same `htn` history, a pressure of 121/61, and the Assessment reads `HTN, controlled (I10)`. The note with nothing to address addressed it; the note with 147/81 did not.

### D6 asks for an order, because for an exposure that is what acting on it is

**D6's fail condition is worded against a naming that is not an acting** — the same trap as D1, from the other end. The submitted note does carry the exposure: the HPI reads `COVID exposure in family`, so a row passing on the finding appearing *anywhere* would score the reference as having addressed it. It ordered no swab. For a documented contact the act is the order, with a named agent and a named specimen in the Plan, which is [#32](https://github.com/mshamblin5150-code/clinical-skills/issues/32)'s rule.

**Two reasons it could have been a B row instead, and why it is not.** It drops out of the *Subjective* rather than the Objective, since an exposure is history and not an exam finding; and what the row checks for is a **generated** order, which is FILLED's subject rather than DRIFT's under [fixtures/README](../README.md).

Both are real and neither moves it. The section it fell out of is incidental — the class is about a documented thing that never reaches the Assessment or the Plan, which is D5's ground for being kept too. And day-b's FILLED class is not generated content in general: it is the vitals license, and B1 through B3 are a chain about one value's plausibility band and what happens when it lands abnormal. An order has no band to land in and nothing to be plausible about. **What D6 tests is whether a given survived**, and the fact that surviving takes the form of an order rather than a sentence is what its fail condition spells out. That is D1's shape, not B2's.

**The DRIFT verdict is not the accusation [#32](https://github.com/mshamblin5150-code/clinical-skills/issues/32) forbids.** The ticket forbids the *skill's note* carrying a FLAG at the clinician on every encounter where he treated a documented contact empirically. It does not forbid this set recording that one encounter fell short — and the standard it fell short of is his own, stated on the ticket: *"you better believe that they are getting swabbed every which way."* A fixture row is read once by him; a FLAG is written into every note.

**Cases 8 and 12 are the in-corpus proof the passing form is writable.** Both document a contact — a daughter-in-law with COVID, a coach sick two days earlier — and both shorthand plans order the swab: `covid` on case 8, `covid, strep, flu` on case 12. Those are *given* orders, so D6 does not fire on them. What they establish is that swabbing a documented contact is this clinician's own practice, and case 9 is where a long shift lost it. `tools/test_corpus_census.py` asserts these two plan lines, and case 9's empty one, rather than trusting this paragraph.

**Case 10 is deliberately not a second row.** Its shorthand reads `has had no sick contacts` and then orders COVID, influenza and strep anyway, so the tests are given whatever the rule does and the case cannot separate a run that reasoned from one that copied. A row needs an input where the rule's output would otherwise be absent, and only case 9 has that.

**What D6 does not hold is #32's other half.** The ticket's complaint was the *route*: the run reached the order as a `FLAG` against the encounter for not swabbing, and then wrote the order to answer the flag. D6 grades the outcome and not the route, for two reasons. A submitted note carries no tier block, so the `Reference did` column has nothing to say about a FLAG that was or was not written. And the rule leaves the flag legal in a narrow case — where testing would have changed the management the encounter recorded — so a row forbidding it outright would fail a run for a judgment the rule permits. The route is a rule in [SKILL.md](../../skills/clinical-note/SKILL.md); the outcome is the bar here.

### D7 is the third lung finding in this set, and it was the one without a row

Cases 1 and 11 each carry a chest exam abnormal that has a row on it — D2's diminished breath sounds in a 24-year smoker, D3's inspiratory wheezing in a documented asthmatic. Case 9's `lung sounds diminished` had none. That is why [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) was found by running the case and handing the output to the clinician, rather than by reading this file: the finding the whole ticket turns on was unasserted.

**It is anchored on the input alone.** `lung sounds diminished` is in the committed shorthand and so is the absent vital line. Nothing here needed a run to write, which is how D1 through D4 were written before the reference was opened, and both of the FILLED rows written the same way fired when it was.

**Case 7 is the in-corpus proof the passing form is writable**, and it is D6's cases 8 and 12 argument on this row. Its exam reads `lungs clear in the apeces, diminished in bases` and its plan orders `cxr` outright. So imaging a diminished lung base is this clinician's own practice on this shift, and case 9 is where a long day lost it. It is also why case 7 cannot host the row: the film is a *given* there, so a run that copied the input would pass having tested nothing — the same reason case 10 is not a second D6. `tools/test_corpus_census.py` asserts case 7's finding and its order, and case 9's finding and its empty plan, rather than trusting this paragraph.

**It now has a reference verdict, and the reference fails it.** The row arrived after the 2026-08-11 read; [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43)'s re-read on 2026-08-13 scored it. `Lungs: diminished;` reaches the Objective and the Assessment names sinusitis and otitis externa — **so case 9's chest finding was dropped by the reference exactly as this row predicted from the input alone.** A row written before the read and confirmed by it is the strongest form this set produces.

### All three lung rows are passable by dismissing the finding, and B9 closes that

D2, D3 and D7 sit on cases 1, 11 and 9 — **every one of them on B1's list**, so every one is handed a complete filled vital set. Each row can then be answered like this:

```
Chest radiograph deferred: afebrile with SpO2 97%.
```

That names the finding in the Plan. **All three rows pass**, and the note has disposed of a documented abnormal on two numbers nobody measured. It is the output [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) recorded verbatim, and the clinician read the finished note and agreed with the reasoning before anyone noticed the values were generated.

Naming a finding and dismissing it on invented grounds score identically here. B9 is the row that separates them, and it is DRIFT's cheat closed from the FILLED side — the same relationship B2 has to B3.

## FILLED — binary, all must pass

A third assertion class alongside DRIFT and REPORTED, defined in [fixtures/README](../README.md). Binary, like DRIFT.

**Enforced rather than counted, deliberately.** day-a holds that *"a bar is only worth having if it was set deliberately"* and left R9 and R10 counted for a stated reason: they turn on differential depth, screening content and education phrasing, which move with the model and the wording. Failing a run over those would be failing it over style.

These sixteen do not move with wording. Each resolves to a value or its absence — is there a blood pressure in the FILLED block, is it below 130 over 80, does the string naming it appear in the Assessment or the Plan, does the given value survive, does every one of the eight OLDCARTS headings carry something, is the severity a number, does a withheld workup cite a value the FILLED block declares, does each stated duration sit beside the symptom the shorthand wrote it beside, does a filled value's line name anything in the encounter, are two filled bodies the same two numbers, does a severity filled for a complaint that does not hurt name the exam finding it came from, does the Plan carry an item for the filled abnormal or the Assessment attribute it to one, does every proposed item appear in the note body. Two runs can word case 9 completely differently and still agree on all sixteen. That is the property that makes a bar enforceable, and it is why these are enforced where R9 was not.

**Three rows need a reader rather than a match, and all three are bounded.** B2's second exit asks whether the Assessment calls the hypertension *controlled*, *treated*, *on therapy*, or names the medication — a code in a pre-existing list is not that, and neither is a monitoring instruction. B9 asks what a withholding decision rests on, and that is answered by joining the block to the sentence: take each decision not to act, list the values it cites, and look each one up in FILLED. **B10 asks which symptom a duration landed on**, and it is bounded twice over: the shorthand names both symptoms and both durations, so the reader is matching four given strings rather than judging how well the timeline reads, and `clinical-note` fixes the shape those strings appear in — `<duration> for <symptoms>`, duration-first, which is what makes the clause boundary findable at all. A symptoms-first value puts the boundary comma among the symptom list's own commas, and then the row would need a reader's goodwill. None of the three needs a taste. B8's second clause is not a fourth — it resolves the way B3's does, by whether something in the Plan answers the number.

**B12 is a fourth and it is bounded differently from the other three**, so it is named apart rather than folded into them. They bound a reader by fixing the strings to be matched; B12 bounds one by fixing where the answer has to come from. The line either names something the encounter contains — a documented condition, a given vital, an exam finding, the treatment, the age — or it names nothing, and *nothing* has a written form here too: the row passes a line that says outright there was no anchor — **for every value but a filled `0/10` severity**, which since [#59](https://github.com/mshamblin5150-code/clinical-skills/issues/59) may not say it, and which no case in this set can raise. **What it never asks is whether the number is a good one**, which is what would need a taste and is not a thing any row in this file does.

**B15 is a fifth, and what bounds it is that both its exits name a section.** The reader is not asked whether the note handled the value *well*: the Plan either carries an education, order or treatment item for it or it does not, and the Assessment either attributes it to something that Plan is treating or it does not. Two things are named as failures rather than left to taste — a recheck of the value itself, and a sentence promising a Plan item the Plan does not contain — and both are checked by looking in the Plan for the thing named. **B16 needs no reader at all**, because it is a count.

**B13 needs no reader at all, and it is the second row in this repo a machine decides.** `filled_vitals_census.py` exits non-zero on it the way `icd10_lookup.py` settles `filled-anchor`'s CODE class. That is not why it is worded the way it is — it is worded that way because *identical* is the only cross-note claim about filled values that does not need a threshold nobody has grounded — but it is why two graders cannot disagree about it.

**Grading the skill is easier than grading the reference**, because the skill's output labels its own filled values in the FILLED block and a submitted note does not. Every `Reference did` cell is written to hold either way — see *The provenance question* above. **Three of B5 through B11 still have no such cell, and it used to be seven.** [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43)'s re-read on 2026-08-13 scored B5, B7, B8 and B10 — the four that turn on what the HPI says, which a submitted note does say. **B6, B9 and B11 are the permanent group**: a submitted note carries no tier block, so no read of it however careful can say which of its values were filled, which of its OLDCARTS elements were invented, or which of its attributions were inferred. The line between the two groups is **whether the row asks what the note says or how the note got there**, and it is not the line this file drew before the re-read: B8 was grouped here with the merely-unscored four and argued *elsewhere* — under *The reference was re-opened for the HPI* — to be unanswerable by any read. It belongs with the readable rows, because the *input* settles its provenance without the block. Two sections disagreeing about one row is how it survived.

| # | Cases | Passes when | Fails when | Reference did |
| --- | --- | --- | --- | --- |
| B1 | 1, 5, 6, 7, 8, 9, 10, 11, 12 | A case whose shorthand carries no vitals is given a **complete filled set** — blood pressure, respiratory rate, height and weight, with BMI derived from the last two — each declared in the FILLED block | Any of the four is left blank, reported under GAPS, or silently omitted | **Produced a complete set on all nine** — blood pressure, pulse, respiratory rate, temperature, oxygen saturation, height, weight and a derived BMI on every one. Not a blank, not a GAPS line. Whether he measured them or supplied them at write-up is unrecoverable and does not bear on this row: something went in every box. Matching this is *neither*; anything less is *worse*. |
| B2 | 8 — 33 F, 9 — 44 F | The filled blood pressure for a patient with **documented hypertension** is either **not normal** — systolic 130 or above, *or* diastolic 80 or above — **or** normal *and* the Assessment names the hypertension as **controlled**, **treated**, or **on therapy**, or **names the medication** | A hypertensive with no recorded pressure is given a normal one and the note says nothing about why it is normal. A code in a pre-existing list is not an account, and neither is a monitoring instruction | **Split — one each way.** Case 9: 132/84, above on both limbs; passes on the first exit. Case 8: **124/78, and the hypertension appears only as `HTN I10` inside a pre-existing code list** — no medication, nothing calling it controlled or treated, and `BP monitors at home` in the Plan says to watch it rather than accounting for the number. Fails on both exits. Matching case 9 is *neither*; beating case 8 is *better*. |
| B3 | 1, 5, 6, 7, 8, 9, 10, 11, 12 | Every **filled** vital or body measurement outside the normal range for that age is named in the Assessment or the Plan | It reaches the Objective and the FILLED block and stops | **Failed it everywhere it fired.** Four filled pressures landed outside normal — case 5 at 132/74, case 7 at 138/82, case 9 at 132/84, case 10 at 126/80 — and not one is named in an Assessment or a Plan. Case 9's filled **BMI of 37.8**, class II obesity, is not coded or mentioned anywhere at all. Naming them is *better*. |
| B4 | 2, 3, 4 | Every **given** vital appears in the note unchanged, and no vital is filled over one the shorthand supplied | A recorded value is replaced, rounded, or duplicated by a generated one | **Failed on case 4.** The given `ht 6'2"` — 74 inches — was recorded as `6'1" (73 in)`, and the BMI of 26.4 is derived from the altered value rather than the given one. Cases 2 and 3 came through unchanged, every value. Beating case 4 is *better*. |
| B5 | 1–12 | All eight OLDCARTS elements — onset, location, duration, character, aggravating, relieving, timing, severity — carry a value, and severity is written as a number out of 10 | Any element is blank, reads `not documented`, is reported under GAPS, or is dropped from the HPI. A severity written as a word — *moderate*, *severe* — fails too | **Failed on all twelve, and not narrowly.** The HPI is one or two lines of compressed prose rather than eight elements. **Severity is a number on four and absent on the other eight** — 8/10, 5/10, 2/10 and 7/10 on cases 1, 4, 5 and 7; nothing on 2, 3, 6, 8, 9, 10, 11, 12. **No case carries all eight.** Case 6 is the floor: its entire HPI reads `No tetanus up to date; no numbness or foreign body`, which carries not one of the eight for the laceration it is about. Filling all eight is *better*. |
| B6 | 1–12 | Every OLDCARTS element the shorthand does not supply is declared in `FILLED·asserted` **carrying its value** | The block names the field without its value, or omits the element. A complete HPI whose FILLED block cannot say which of the eight were invented fails | **Unscoreable against the reference, permanently.** A submitted note carries no tier block, so it declares no OLDCARTS element and names no value as invented. Same position as B9, B11, B12, R3 and R4 — and **the only one of B5 through B8 in it**. |
| B7 | 1, 2, 4, 5, 7, 8, 10, 11, 12 | A **given** severity survives. The seven cases that write a score carry that number unchanged — 8, 5, 2, 7, 8, 8 and 6 out of 10 on cases 1, 4, 5, 7, 8, 10 and 11 — and the two that write `no pain` are scored **0/10** | A written score is rounded, moved, or replaced by a generated one; or a documented absence of pain is scored above 0/10 | **Failed on three of the seven — by dropping, not by overwriting.** Cases 1, 4, 5 and 7 carry their given scores unchanged: 8/10, 5/10, 2/10, 7/10. Cases 8, 10 and 11 carry no score at all — `c/o 8/10 body aches`, `8/10 weaknes` and `pain is exacerbated by movment 6/10` reach the note as body aches, weakness and a draining cyst with no number attached. Both absences survive — case 2 writes `painless unless bumped`, case 12 `denies significant pain`, neither scored above 0/10. Carrying all seven through is *better*. **The verdict comes off *Passes when*, because *Fails when* has no limb for a dropped score** — it names rounding, moving and replacement, and both were written expecting a run to overwrite a given rather than lose it. That is a gap in the row: [fixtures/README](../README.md) asks a binary row's pass and fail be complementary, and this one's are not. B5 catches the drop, so nothing goes unscored — the row is under-specified rather than the set blind. Listed under *Still unresolved*. |
| B8 | 6, 9 | The severity is **filled**, lands **above 0/10**, and something in the Plan answers it — an analgesic, or the treatment of what is causing the pain | It lands at 0/10 for a patient with a sutured laceration or worsening facial pain; or it reaches the HPI and the FILLED block and the Plan responds to it with nothing | **Failed on both, and the read settles it — which this file previously said no read could.** Neither note carries a severity anywhere: case 6's HPI is two negatives about tetanus and foreign body, case 9's is a COVID contact and a home medication. **No tier block is needed to establish these would have been filled** — the shorthand supplies neither a score nor an absence for either case (`DAY_B_SEVERITY_FILLED` in `tools/test_corpus_census.py`), so any number there is necessarily invented, and there is none. The row fails on its first clause and the Plan clause never fires, though both Plans do answer the pain: ibuprofen 400 mg on case 6, the sinusitis and otitis externa treated on case 9. Filling a score above 0/10 and answering it is *better*. |
| B9 | 1, 3, 5, 6, 7, 8, 9, 10, 11, 12 | Every reassurance in the note traces to a **given**. No decision to withhold, defer or narrow the workup of a documented finding rests on a filled vital, body measurement or pain score, and any cause a filled abnormal is attributed to is a **given finding** | A filled normal is the ground for not acting: `Chest radiograph deferred: afebrile with SpO2 97%` on a case whose shorthand records no temperature and no saturation. A filled abnormal attributed to a filled cause fails too — a filled fever accounting for a filled pressure is both halves of one reassurance invented | **Unscoreable against the reference, permanently.** A submitted note carries no tier block, so nothing in it says which of its values were filled, and a reassurance resting on one is unreadable either way. Same position as B6, B11, B12, R3 and R4. See below. |
| B10 | 8, 9 | Both stated durations reach the HPI attached to the symptoms they were written beside, each written **`<duration> for <symptoms>`**. The chief complaint's `x 2 days` covers the symptoms the chief complaint lists, and the second onset statement covers its own symptom — case 8's **right earache**, case 9's **facial pain** | Either duration is missing from the HPI; or the chief complaint's two days is applied to the symptom the second statement dates, collapsing two timelines into one. **A note that carries both durations and attributes neither fails** — see below | **Failed on both, and case 9 is this row's collapse in the opposite direction.** Case 8 keeps one and loses the other: the HPI reads `2 days of viral sxs` and the earache's *yesterday* appears nowhere — the earache surfaces only as a right TM finding and an AOM diagnosis. **Case 9 carries neither duration in the HPI**, and its chief complaint reads `Pressure in face, earache, cough/sneeze since yesterday` — the second statement's *yesterday* applied to all three symptoms, with the chief complaint's two days lost outright. This row anticipated the two days swallowing *yesterday*; the reference swallowed the two days instead, and both are one timeline where the shorthand wrote two. Attributing both is *better*. |
| B11 | 9 | The attribution of `states this started yesterday` to the facial pain is declared in `FILLED·asserted` **carrying its value** — which symptom, which duration | The attribution is made and not declared, or the block names the element without the value. A run that dodges the declaration by not attributing at all has already failed B10 | **Unscoreable against the reference, permanently.** A submitted note carries no tier block, so nothing in it says which of its attributions were inferred — and case 9's bare `this` is exactly the attribution that has to be inferred rather than read. Same position as B6, B9, B12, R3 and R4. See below. |
| B12 | 1–12, on whichever values each note declares filled | Every filled vital, body measurement and severity's `FILLED·asserted` line **names what the value was reasoned from** — a documented condition, a given pulse or temperature, the exam's description of distress, the treatment given, age and sex — **or says the encounter supplied no anchor for it**, **which every value but one may say** — a filled `0/10` severity may not, and names the search instead | Any filled value's line carries the value alone. **A line naming a category rather than an anchor fails too** — `reasoned clinically`, `age-appropriate`, `plausible for the presentation` name nothing in this encounter. A line saying `no habitus or percentile datum in the source to move it` **passes**. **A filled `0/10` severity saying it had no anchor fails**, because for a zero that sentence and the honest one are the same sentence — no case here can raise it, see below | Unscoreable against the reference, permanently. A submitted note carries no tier block, so it declares no value and names no reasoning about one. Same position as B9, R3 and R4. |
| B13 | **the set** | **No two of the nine filled cases carry an identical height-and-weight pair.** Settled by `python tools/filled_vitals_census.py <the run directory>`, which exits non-zero when two do | Any two of the nine filled bodies are identical. **Run 2 fails it**: cases 1 and 5 both carry `5'10" / 190 lb`, at ages 36 and 68 | **Passed — nine distinct bodies.** 5'7"/145, 5'10"/180, 5'10"/150, 5'4"/160, 5'5"/175, 5'4"/220, 5'8"/180, 5'11"/185 and 5'6"/125 on cases 1, 5, 6, 7, 8, 9, 10, 11 and 12. **Two heights and one weight repeat, and no pair does** — 5'10" on cases 5 and 6, 5'4" on 7 and 9, 180 lb on 5 and 10. **What produced that is not readable and this row does not need it**: a submitted note carries no tier block, so whether he reasoned each body or reached for a familiar number is exactly what B1's cell calls unrecoverable. The row asks only whether two bodies coincide, and none do. Matching him is *neither*; run 2's repeated `5'10" / 190 lb` is *worse*. |
| B14 | 3 | The complaint is **not a painful one** and the shorthand writes neither a score nor its absence. The severity is filled, lands **above 0/10**, and carries the complaint as its label — `N/10 itching` in form, never `N/10 pain` and never `N/10` alone. **The number itself is not pinned**: any score its anchor supports passes, on B8's precedent. Its `FILLED·asserted` line names a **pain source in the exam**, in the shorthand's own terms: the scattered abrasions, the scratch marks in both ear canals | It lands at 0/10 for a woman whose exam describes broken skin in two places; or it lands above 0/10 with the line saying the encounter supplied no anchor, which for this case is false and makes the number an invented abnormal; or the value reads as a bare `N/10`, or labels itself `pain`, either of which loses the complaint the score belongs to | **Failed, and scoreable for the reason [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43) gives for B8.** Provenance is settled from the input side: `DAY_B_SEVERITY_FILLED` pins case 3 as supplying neither a score nor an absence, so any severity in the note is necessarily filled and no tier block is needed. **The submitted note carries none** — B5's cell lists case 3 among the eight with no severity at all. Filling it is *better*. **This cell first read *unscoreable, for B12's reason*, which is the exact claim #43 withdrew**, written on a branch that could not see it. |
| B15 | 1, 5, 6, 7, 8, 9, 10, 11, 12 | Every **filled** vital or body measurement outside the normal range is **addressed**, and addressed has two exits: the **Plan** carries **education, an order or treatment** for it, or the **Assessment attributes it to something the Plan is already treating** | Nothing in the Plan answers it and the Assessment attributes it to nothing the Plan treats. **A recheck of the value is not an address** — `confirms the height and weight` at follow-up re-measures the number rather than doing anything about it. **And a sentence promising a Plan item the Plan does not contain is not an address**: `addressed with nutrition and activity counseling at the primary care follow-up`, in a note whose Plan carries no weight, nutrition or activity line | **Failed, and the verdict follows from B3's read rather than from a fresh one.** B3's cell records four filled pressures — 132/74, 138/82, 132/84, 126/80 — *"not named in an Assessment or a Plan"* — and case 9's filled BMI of 37.8 *"not coded or mentioned anywhere at all."* A value named in neither section fails B3, and **B15's pass set is a subset of B3's**, so the failure follows from what the 2026-08-11 read already recorded. Provenance needs no tier block: these nine shorthands carry no vital line, so every one of those values is necessarily filled. Addressing them on either exit is *better*. |
| B16 | 1–12 | **Every `FILLED·proposed` item appears in the note body**, and the count says where. List the block's proposed items, then name where each landed — the Plan order, the education point, the follow-up interval, the results line. **Two integers that either match or do not** | Any proposed item landed nowhere. **A `FLAG`, a `GAPS` line or an `UNKNOWN` token is not a landing**, and neither is the item being restated in the drift matrix. **Run 3's case 7 is the worked failure**: `FILLED·proposed  Nutrition and activity counseling for the overweight body mass index`, against a Plan whose `Nonpharm`, `Pharm`, `Education` and `Follow up` lines carry no weight, nutrition or activity item at all | **Unscoreable against the reference, permanently.** A submitted note carries no tier block, so it proposes nothing and there is no list to count. |

### B15 and B16 are one ruling, and they catch the two halves of it

[Issue #47](https://github.com/mshamblin5150-code/clinical-skills/issues/47) is the defect this set has reproduced on three runs and three cases — run 1 case 5, run 2 case 10, run 3 case 7 — and **B3 survived every one of them**, because B3 is a disjunction and on two of the three the Assessment named the value. The clinician ruled the mechanism on **2026-08-15**, and these are the two rows it produces.

**The title named two defects and they came apart.** Sorted by instance:

| Instance | Reached Objective and stopped | Self-report falsely claimed a pass |
| --- | --- | --- |
| run 1 case 5 | yes — named nowhere | yes |
| `peds-bp` case 2 | yes — named nowhere | yes |
| run 2 case 10 | **no** — Assessment named it | yes |
| run 3 case 7 | **no** — Assessment named it | yes |

The first clause fired twice, in the earliest evidence, and **B3 scored it correctly both times**. The second is four for four and **nothing in this repo scored it at all.** So B16 is where the enforcement went, and B15 is the clinical rule it rests on.

**B15 is not B3 rewritten, and that distinction is the whole reason it is a new row.** Editing B3's text would redate three recorded scores — `FILLED 3/4`, `9/9` and `10/11` each contain a B3 verdict awarded against the disjunction — and this file's convention is **append, never insert** for exactly that reason. B3 stays as written and is now the weaker precondition: a value named nowhere fails both, a value named only in a promise fails B15 alone. That is B1 → B2 → B3's structure and B5 → B8's, one more time.

**B16 is not B15 with the block in place of the body**, and neither contains the other. B15 reads the note against the rule; B16 reads the block against the note. **A run can pass B16 and fail B15** — propose nothing it did not write, and still let the Assessment claim a Plan item that was never proposed in the first place, which is run 2's case 10. **And it can pass B15 and fail B16** — address the BMI properly in the Plan while dropping a proposed swab, a proposed referral or a proposed return-precaution line, which is the wider class B16 reaches and #47 did not.

**B16 is counted rather than quoted, and the alternative was live.** The other candidate was to require the drift-matrix verdict to quote the Assessment or Plan words carrying each filled abnormal. **It would have passed case 7**: *"Addressed with nutrition and activity counseling at the primary care follow-up"* is a real string in that note, quotable, and false. The most rigorous-looking check does not catch the most recent recurrence, because the false sentence is already inside the note. Two integers do not have that problem, which is [fixtures/README](../README.md)'s own criterion for what a binary row may rest on.

**Neither row has a value, and both are known to fail before anyone scores them.** That is G1's position and B12's — the rows were written from run 3's own output, and `clinical-note` gained drift rows 4-as-narrowed and 21 with them, so twelve notes predating the change would produce a number belonging to neither commit. **The convention is applied here at its weakest for B15 and at full strength for B16.** B15's prohibition was arguably in the file already — drift row 4 has always said *addressed somewhere, not just recorded* — and what was missing was any statement of what *addressed* means. **B16's was not in the file at all**: nothing anywhere required a `FILLED·proposed` item to reach the note body, so run 3's case 7 was compliant with everything written down. That is row 20's position rather than row 17's, and it is why the skill file gained a paragraph under *Emit the tier block* alongside the row.

**B16's `Reference did` cell joins the permanent group and the roster question is not reopened here.** B6, B9, B11, B12, R3 and R4 all record *unscoreable, permanently* because a submitted note carries no tier block, and B16 is the seventh. Their `Same position as` lists are already uneven and [#117](https://github.com/mshamblin5150-code/clinical-skills/issues/117) is where that gets settled; this row states its own reason and names no peers rather than adding an eighth point-in-time list to the pile.

### B1 through B3 are a chain, and each closes the one above

B3 is what [issue #8](https://github.com/mshamblin5150-code/clinical-skills/issues/8) asked for. On its own it is **passable by cheating**, and each cheat is the exact behavior the license forbids.

**B3 alone is passable by filling a bland value.** `clinical-note` requires that a filled vital be *"the value this patient most plausibly had … not from the middle of the normal range"*, and that one landing abnormal be worked up in full. But whether it lands abnormal is itself generated. A run that fills 118/76 for case 9 — 44, hypertensive, with peripheral arterial disease — has nothing abnormal to address, so B3 passes with nothing having been tested. The note that manufactured a bland vital scores identically to the one that reasoned.

**B2 closes that**, and its threshold is deterministic: `clinical-note` sets normal below 130/80 and `corpus_census.py::is_normal_bp` encodes the same as an explicit conjunction, so a split reading like 132/78 is unambiguously *not* normal.

**What B2 does not do is demand an abnormal number**, and an earlier version of it did. A treated hypertensive at 124/78 is not implausible — that reading is what the treatment is *for* — so a row insisting on an abnormal pressure would have been ordering up an invented abnormal finding, exactly what standing rule 2 forbids. The bland-fill cheat is still real; what distinguishes it from good care is not the number but whether the note accounts for it. Hence the second exit, and hence the only thing B2 actually forbids: **a normal pressure in a hypertensive, unexplained.**

**B2 alone is passable by filling nothing at all.** This is the same defect one level up, and it is the one worth stating plainly: B2 and B3 are both conditional on a value having been generated. A run that leaves case 9's blood pressure blank, or reports it under GAPS, has no filled pressure to be normal or abnormal — and passes both rows having tested nothing.

**B1 closes that**, and it is the row the license itself demands: *"A value is required … Something has to go in the box."* Medatrax holds fields for blood pressure, respiratory rate, height and BMI, so those are what B1 names. It is the precondition the other two rest on, which is why it comes first.

Cases 8 and 9 are B2's anchors — both carry `htn` in the history and neither carries a single vital. Case 9 is the stronger: hypertension plus peripheral arterial disease plus hyperlipidemia at 44 makes an *unexplained* normal pressure incoherent. Not an impossible one — she could be well controlled — but a note that produces 118/76 for her and then says nothing about how she got there has not reasoned, it has guessed.

**The reference read confirms the chain was not theoretical.** The clinician cleared B1 on all nine, then produced the two failures the chain predicts: an unaccounted-for normal pressure in a hypertensive (case 8, B2) and four abnormal filled pressures that reach the Objective and stop (B3). Both rows were written into this set from the inputs before the reference was read. Both fired.

**B1 and B3 still leave the other seven cases vacuous-passable on the vital half.** Cases 1, 5, 6, 7, 10, 11 and 12 must now be filled completely, but nothing forces any of those filled values abnormal, so B3 can pass on them without firing. Only cases 8 and 9 are reached at all, and B2 no longer forces even those to be abnormal — it forces them to be *explained*. That is a narrower hole than before and it is not closed.

### B5 through B8 are the same chain on the OLDCARTS half

[Issue #30](https://github.com/mshamblin5150-code/clinical-skills/issues/30) ruled that no OLDCARTS element may be blank and that severity is always a pain scale, taking the filled-vital treatment rather than the filled-finding one. These four are that ruling, and they are B1 → B2 → B3's argument again with the severity in the pressure's place.

**B8 alone is passable by filling nothing at all.** A run that leaves case 9's severity out, or writes `not documented`, has no score to be above 0/10 and passes having tested nothing. **B5 closes that**, and it is the row the ruling itself demands: eight, always eight.

**B5 alone is passable by writing 0/10 everywhere.** A blank is not the only way to say nothing — a note that scores every complaint at zero has satisfied every box while describing twelve patients in no pain, and one of them came in with a thumb laid open. That is *filled content is unremarkable* applied to a scale where unremarkable is a claim, which is exactly why the ruling put the severity in the vitals class. **B8 closes it**, on the two cases where the shorthand is silent and the complaint is unarguably painful.

**B5 and B8 together are passable by overwriting the givens.** Seven of these twelve write a score and two write the absence of one, so nine of the twelve have nothing to invent — and a run that "fills" a severity over `c/o 8/10 pain` has replaced a transcribed value, while a run that scores case 12 at 5/10 against `no pain` has invented a symptom outright. **B7 closes both**, and it is B4's job on this half: a given survives.

**B5 leaves the reader unable to tell which of the eight were invented**, which is the defect the FILLED block exists to prevent — the note body is written so given and filled content read identically. **B6 closes that**, on the rule `clinical-note` already states for a filled vital: the block carries the value, not the field name.

**The case lists are computed, not eyeballed.** `tools/test_corpus_census.py::DayBIsTheAbsenceSet` asserts the split from the committed inputs — which seven write a score and what it is, which two write `no pain`, which three write neither — so adding a score to one of these files fails a test instead of quietly voiding a row. The extractor it uses had a real bug that the guard found: two of the seven write the score at the end of a sentence, and the first version threw them away.

**Case 3 was the boundary, and she is now B14's.** For as long as this section has existed, B5 was the only one of these rows reaching her: her complaint is itching, #30 was raised about a patient in pain, and a row scoring her would have been this set enforcing an extension rather than a ruling. [Issue #42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) made the ruling, so the row exists — see below.

### B14 is the ruling #42 made, and the ticket described none of it

[Issue #42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) asked what the severity box reads for a complaint that does not hurt, and offered `0/10`, `4/10 itching` on the argument that the scale grades the complaint's intensity, or both. **The clinician's answer picked the middle value and rejected its argument.** The scale scores **pain** — always, with no second form for a non-painful complaint — and the words after the number name the **complaint** rather than what is scored. `6/10 facial pressure` is 6/10 pain on a complaint of facial pressure; `4/10 itching` is 4/10 pain on a complaint of itching. So `clinical-note`'s sentence was corrected rather than confirmed, even though the value it produced for this case was right.

**The ruling is grounded in the encounter, and the fixture is deliberately unchanged.** He asked her about pain, she answered 4/10, and she named itching as what she mostly had. `shorthand/case-03.md` records none of that, and **it stays that way**: an input here comes from the day file and from nowhere else, which is this set's oldest rule. Writing the score in would move her from the three cases that invent a severity to the seven that transcribe one, and the only fixture in this repo testing a non-painful complaint's filled severity would be the one deleted to record the answer.

**B14 does not pin the number, and B8 is the precedent.** The run reasons its way to a value; 3/10 and 5/10 are as defensible from this exam as 4/10, and a row demanding the clinician's own figure would fail a correctly reasoned note. What it pins is the three things that are checkable without being him: above zero, labeled with the complaint, and anchored in the exam.

**That third limb is not B12 repeating itself.** B12 asks every filled value's line to name what it was reasoned from **or to say the encounter supplied no anchor**, and the second exit passes. For case 3 the second exit is *false* — the abrasions and the scratch marks in both canals are sitting in the shorthand — and a run taking it has written an above-zero score with nothing behind it, which is the invented abnormal `clinical-note` forbids under *Which value was chosen is the instruction*. B14 closes that exit on the one case where it is unavailable.

**B14 is no longer the only place that exit is closed, and the two closures are different shapes.** [#59](https://github.com/mshamblin5150-code/clinical-skills/issues/59) closed it on 2026-08-15 for a filled `0/10` severity anywhere, because for a zero the exit and the honest answer are the same sentence. **B14's closure is about one patient and #59's is about one value**, and neither implies the other: case 3 scores above zero and still has to name her anchor, while a zero in some other encounter has to name a search that found nothing. This set can score the first and not the second.

**The reference left her severity blank, and that is the ruling's other half arriving from the opposite direction.** [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43)'s re-read landed on `main` the same day this row was written and neither branch could see the other. It records that four of the twelve submitted notes carry a numeric severity and eight carry none, **case 3 among the eight** — so the clinician asked her about pain, got a number, and charted no severity at all. **Those two facts are not in tension and the second is why the first had to be asked for**: *Silence is undocumented, never absent* is the whole of it, and a note that fills the box is doing what the submitted one did not. It is also why the ruling could not be read off the corpus, which #42 said at the top.

**And it is why case 3 did not simply join B8.** B8's two are complaints that unarguably hurt, where the run has only to not write zero; its failure column names a sutured laceration and worsening facial pain, and would have had to be blurred to hold her. Case 3's difficulty is upstream of that — the complaint gives her no pain at all, and the number has to come from the exam or not at all. `tools/test_corpus_census.py::DayBIsTheAbsenceSet.test_b8_and_b14_partition_the_three_filled_severities` asserts the two rows split the three invented severities with no overlap and nothing left over, so a later tidy folding them together fails a test rather than quietly dropping the distinction the ruling turns on. A second test pins the two exam strings B14's anchor limb rests on.

### B9 is the subtractive half of the license, and nothing else here reaches it

B1 through B3 and B5 through B8 are all one direction: a value gets generated, and the rows ask whether the note then did enough work. B9 is the other direction, and it is the one [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) found — a generated value letting the note do **less** work. `clinical-note` states it as *a filled vital may raise an obligation and may never discharge one*.

**B9's ten cases are a union, not B1's nine.** The row reaches a case where *anything* in the license class was generated, so it is the vital-less nine plus **case 3**, whose vitals are complete and whose OLDCARTS severity the run has to invent — `DAY_B_SEVERITY_FILLED` in `tools/test_corpus_census.py`. Cases 2 and 4 are the only two outside it: both carry a full vital line and both settle the severity in the shorthand — case 4 with a score of 5, case 2 by writing `no pain`, which is a given scoring 0/10 rather than a value to invent. So a run has nothing generated to reason from and the row has nothing to check. Case 3 is easy to lose here, and the first draft of this row did lose her — she is B1's counter-example and B9's member at once, which is exactly the kind of split the case lists are computed rather than eyeballed to catch.

**No row above fires on it, which is worth being exact about.** Take the run [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) reports, which filled case 9's `T 98.8 F` and `SpO2 97%`, both squarely normal. B1 passes — the box has a value. B3 never fires — the value is not abnormal, so there is nothing downstream to have lost. B2 reaches the pressure and says nothing about the other four. Every FILLED row in this set is satisfied by the run that deferred a chest film on two numbers nobody measured.

**And a filled normal is the harder case, not the easier one.** An invented abnormal has to be worked up and draws attention to itself doing it. An invented normal is what makes a workup stop, and it stops it in a sentence that reads like good clinical judgment. That asymmetry is why the row is worth its own line rather than a clause bolted onto B3.

**Two independent runs of case 9 both made the move, which is what takes B9 past a single anecdote.** The run [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) reports filled `T 98.8 F` and `SpO2 97%` and deferred the film on them. **day-b run 1 filled different numbers and arrived at the same place** — it is committed, as [filled-anchor/notes/case-09.md](../filled-anchor/notes/case-09.md), and its Assessment reads:

> There are no wheezes, no crackles, no rhonchi, no shortness of breath and no hypoxia (SpO2 96% on room air, RR 18). Most consistent with reduced air movement in the setting of body habitus (BMI 35.2) plus the current acute upper respiratory illness, rather than a lower respiratory process.

Its own FILLED block declares all three numbers: `RR 18 filled`, `SpO2 96% on room air filled`, and a BMI derived from a filled height and a filled weight. **Every *number* in that sentence was generated**, and what they carry is the conclusion that the documented finding is not a lower respiratory process. No film is ordered; spirometry is deferred to *"once she is well."*

**The absent adventitious sounds in the same sentence are a different matter, and the row must not claim them.** `no wheezes, no crackles, no rhonchi` are filled *findings* — the shorthand mentions none of them — and `clinical-note` rules that filled findings keep their full force, because silence about a finding is evidence where silence about a vital is not. They may reassure. So the sentence is part legitimate reasoning and part invention, and B9 fails it on the numeric clause alone. That is the row at its narrowest, which is where it should be read.

**That run is the strongest argument for the row, because it is the careful one.** It heads the paragraph *"Diminished breath sounds — addressed, not filed"*, it names the finding, and it reaches the Assessment and the Plan — so D7 passes on it, and so do B1 and B3. Its own drift matrix records `Row 4 — Vitals: PASS`. A note doing everything the set asked still disposed of a documented abnormal on three invented numbers, and nothing in this file could see it. B9 is what sees it.

**What B9 catches is the stated dismissal, and it does not catch silence.** A run that simply orders no film, and says nothing at all, cites no value for the row to look up. That hole is real and it is covered here by the DRIFT side rather than this one: D2, D3 and D7 each demand their lung finding reach the Assessment or the Plan, so silence fails there. **The two halves are complete only together** — DRIFT forbids dropping the finding, B9 forbids disposing of it on an invented ground, and a run has to clear both.

**No reference verdict, and for a different reason than B5, B7, B8 and B10 went without one.** Those four were merely unscored until [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43)'s re-read, and it scored them. B9 could not be scored by any read at all: a submitted note carries no tier block, so nothing in the reference says which of its values were measured, and *"deferred, afebrile"* in a portal note is unreadable either way. This is the asymmetry *Grading the skill is easier than grading the reference* records, in the one place where it is total rather than merely inconvenient.

### B10 and B11 are the two limbs of one rule, and this shift already held both

[Issue #33](https://github.com/mshamblin5150-code/clinical-skills/issues/33) was raised on case 9, where a run carried `cc: … cough x 2 days` and `exam: states this started yesterday` into the note verbatim and raised a FLAG saying the two disagreed. The clinician's ruling was that it be **resolved, not flagged**, and what resolves it is that the two durations were never about the same thing: a duration belongs to what it is written next to. `clinical-note` states that as drift row 16.

**The rule has two limbs and case 9 exercises only one of them.** Its second onset statement is a bare `this`, so attaching it to the facial pain rests entirely on `pain inface is worse` in the next clause — an inference, and the one call in the rule that can be wrong. **Case 8 is the other limb**, and it was already in the set: `cc: ha, fever, cough, sore throat, weakness x 2 days` with `right earache yesterday` in the exam. That names its own symptom, so attaching it is reading rather than inferring, and no declaration is owed. B10 spans both; B11 reaches only case 9.

**Case 8 is the stronger of the two for the drop, and case 9 for the declaration.** The earache dated to yesterday is what the shorthand's own `right AOM` rests on, so a run that folds it into the chief complaint's two days has misdated the diagnosis it went on to write. Case 9's facial pain is what an acute bacterial rhinosinusitis argument would turn on — *worsening after initial improvement* is one of the two criteria, and Augmentin 875 was prescribed. Neither row scores the antibiotic or the otitis, deliberately: what these check is the timeline, and a row demanding the Assessment argue about the drug would fixture a clinical rule nobody ruled on. That is the same line day-b already draws around case 10's steroid-in-a-diabetic, under *Still unresolved*.

**B10's fail condition is worded against carrying both and attributing neither**, and run 1's committed case 9 output is why. That note is [filled-anchor/notes/case-09.md](../filled-anchor/notes/case-09.md), and it contains **both strings and no attribution**:

> Onset — symptoms began yesterday per the history, while the chief complaint records two days; both durations are as written and are not reconciled in the source

A row passing on `yesterday` and `two days` both appearing would score that as having read the input. It had not — it says outright that it did not, and then raises a FLAG for it: `Duration stated two ways - the chief complaint says 2 days, the history says it started yesterday. Both are given, neither is reconciled in the source, and both are carried forward rather than one being chosen`. The row asks which **symptom** each duration landed on, which is D1's shape: naming is not acting.

**That run is the strongest argument for the rows, because it half-solved it.** Its Duration element already reads `1 to 2 days, continuous`, and its differential argues from *"the 1-to-2-day onset"* — so the span form was in reach without anybody stating it, which is what makes row 16's residual limb cheap rather than novel. What it never did was ask *whose* two days and *whose* yesterday. The facial pain is dated in the same clause it is dated by, and the run put `Timing — constant, and the facial pain is worse today` three elements away without connecting the two. B10 is what connects them.

**And its Onset value is the row 12 shape, which is a second finding rather than this row's business.** *"both durations are as written and are not reconciled in the source"* is a sentence in the note body accounting for the note's own construction — strike the two durations and it has nothing left to do. Recorded here because it is the same sentence B10 exists to make unnecessary, and left to row 12 to score.

**Both are B rows and neither is a D row, and B10 is the one where that needs an argument.** B11's subject is generated content declared in the block, which is FILLED's on any reading. B10's subject is a *given* surviving, which is DRIFT's — and it is a B row anyway for the reason B1 is: **it is B11's precondition.** A run that never attributes anything has no inference to declare, so B11 passes having tested nothing; B10 is what forces the attribution to exist. That is B1 → B2's structure and B5 → B8's, one more time. The DRIFT bar would not have fitted it either: D-rows pass when the finding reaches the **Assessment or the Plan**, and what B10 checks lands in the HPI.

**Cases 4 and 12 restate a timeline and agree with themselves**, which is why B10 sits on two cases rather than four. Case 4 writes `fever x 5 days` and then `this has been on going x 5 days`; case 12 writes `started saturday` and then `states started saturdy`. A run that ignored row 16 entirely would still produce the right number on both, so neither case can separate a run that attributed from one that copied — the same reason case 10 is not a second D6 and case 7 cannot host D7. `tools/test_corpus_census.py` asserts the agreement rather than trusting this paragraph, so an edit that made either case disagree surfaces as the new row it would be.

**Row 16's span limb has no anchor in this set, and it is now fixtured elsewhere.** None of the twelve carries two durations for the *same* symptom, which is the case where the rule says to write a span containing both — cases 8 and 9 date a different symptom, cases 4 and 12 restate a timeline and agree with themselves, and the other eight state one timeline or none. So this set covers the attribution limb only, and that is a property of the shift rather than a gap anyone can close here. [#65](https://github.com/mshamblin5150-code/clinical-skills/issues/65) closed it with a set of its own: [duration-span](../duration-span/assertions.md), three encounters out of a 43-encounter pool, whose S1 asks that the note's duration element carry **both** stated values.

**Two of these twelve are in that set's pool and neither is in the set**, which is the sharpest available statement of what B10 and this repo's newest rows are each for. Cases 8 and 9 carry two different durations sharing a symptom word, so `tools/corpus_census.py`'s filter admits them — and reading them is what says they are the attribution limb, where there is no conflict and nothing to span. **The machine cannot make row 16's own distinction**, which is why `duration-span` publishes a pool and picks by reading, and why that filter was not tuned until it stopped returning these two.

**The two rows are in different positions, and the re-read confirmed it.** B10 was readable off a submitted note the way D1 through D4 were — whether the HPI dates the earache to yesterday and the rest to two days is right there in the prose — and [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43) scored it on 2026-08-13: **the reference fails on both cases.** **B11 can never be scored from the reference at all** — a submitted note carries no tier block, so nothing in it says which attributions were inferred. That is B9's position, and it is the asymmetry *Grading the skill is easier than grading the reference* records.

### The reference was re-opened for the HPI

The 2026-08-11 read answered the vital and drift questions this set was built on and was never scored for OLDCARTS. The twelve notes in `scratch/day-b-reference/` were re-opened for the HPI on **2026-08-13**, [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43), and **seven rows that carried no verdict now carry one**: B5, B7, B8, B10, B13, D7 and R2. **B6 is the eighth and the read could not settle it** — it asks about a `FILLED·asserted` block a submitted note does not have, so its cell now records that rather than waiting for another session.

**No row is waiting on a portal session any more.** Nine rows were, and the same session cleared the last two: **C1 and C2 were scored on this read as well**, which is beyond what [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43) asked for and is why the *Still unresolved* bullet owing them is gone. What remains unscoreable is unscoreable by construction rather than by nobody having looked — **B6, B9, B11, B12, R3 and R4** because a submitted note carries no tier block, **R5** because it asks for a share of *his* filled values and nothing marks them. **B9's cell read `Not scored. See below.`** where B12's and R3's said *unscoreable* outright — the wording B5, B7, B8 and B10 carried while they were merely unread, left describing a permanent state in the language of a temporary one. #43 saw it and left it alone, because rewording a row inside the change that scored eight others is how a fixture set loses the ability to say what it tested. [#112](https://github.com/mshamblin5150-code/clinical-skills/issues/112) reworded it to their form on 2026-08-13 and touched nothing else: the prose under the row was already correct, no `Passes when` or `Fails when` moved, and no run's score changed.

**B11's cell was the last one not in that form, and it was a weaker case than B9's.** It read `Not scored, and unscoreable. See below.` — which did say unscoreable, so no reader took the row for one still waiting, but it led with the retired half where its five peers lead with the verdict. [#115](https://github.com/mshamblin5150-code/clinical-skills/issues/115) reworded it to the group's form on 2026-08-13 and touched nothing else: `Passes when` and `Fails when` are unmoved, the prose beneath the table was already correct and is more specific than any cell, and the row's verdict did not change — it said unscoreable before and says it now — so nothing is owed a re-score. **The `Same position as` lists are still uneven**: B6's, B9's and now B11's name five peers each, B12's three, R3's one, each written when the group was smaller and none revisited as it grew. None of them is wrong and reconciling them is a separate question, named rather than answered — #115 needed its own list to be right and needed nothing from the others. [#117](https://github.com/mshamblin5150-code/clinical-skills/issues/117), `grilling`: whether the file wants one roster, six point-in-time statements, or no lists at all is a decision nobody has taken, and closing it as not worth six edits is one of the answers.

**The reference fails six of the seven — B13 is the only pass — and B5 is the one the ticket predicted.** Its HPI is one or two lines of compressed prose on every case, not eight elements: four of the twelve carry a numeric severity and eight carry none. That is the shape #43 was filed hoping for, and *Reading it moved this set in both directions* is why it matters — a bar the reference clears everywhere is a bar set too low, and this is one it clears nowhere.

**One claim this section used to make is withdrawn, and it was wrong in a way that looked right.** It read that B8 *"asks what the run did with a number it invented, and a submitted note could not answer that even if it were read — it does not label its filled values."* The premise is true and the conclusion does not follow. Provenance for B8's two cases is settled **from the input side**: `DAY_B_SEVERITY_FILLED` in `tools/test_corpus_census.py` pins that cases 6 and 9 supply neither a score nor an absence, so any severity in the note is necessarily filled — no tier block required. The reference wrote none, and that alone fails the row. **B6 is the one that genuinely needed the block**, and folding it in with B8 is what hid the difference for as long as it did.

**The two things [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43) named as cheap and not required**, both recorded rather than graded:

- **Severity is written as a number or not at all.** Never as a word. The eight cases without a score do not say *moderate* or *severe*; they say nothing, or they report an absence in prose — `painless unless bumped`, `denies significant pain`. So B5's word-severity limb has no reference instance, and the failure it does catch is the blank.
- **The arrow [#31](https://github.com/mshamblin5150-code/clinical-skills/issues/31) forbids is in the reference, and this read did not find it.** Case 10's Plan reads `Mg 1.6 → recommend OTC magnesium supplement`, and **D5's cell has quoted that string since the 2026-08-11 read** — what nobody had done is hold it against #31, which landed later and rules against exactly this construction on a run's `Wt 198 lb -> BMI 34.0`. So the new fact is a collision between two things already written down, not an observation. **Held loosely either way**: these files are transcriptions, and whether the arrow is his or the reader's rendering is not recoverable from them. No row turns on it.

### Case 2's height: resolved, and B4 now covers it

Case 2's vital line reads `wt 62in wt 131` — `wt` written where `ht` was meant, the same defect day-a case 8 preserves as `hr 65 inches`. The set previously exempted case 2's height from B4, because the value was *given* under one reading and *absent* under the other and B4 would have punished a run for either choice.

**The reference settles it: 62 is a height.** The submitted note writes `5'2", 131 lb, BMI 24.0`, the portal Height field reads 62, and 131 lb at 62 in recomputes to 23.96 — so the BMI is derived from that reading and not from some other pair. The clinician was in the room; what the patient's height was is a fact he had and the shorthand mistyped.

**So the exemption is withdrawn and B4 covers all of case 2**, height included. A run that reads `62in` as a weight, or that treats the height as absent and fills one over it, fails B4. The suffix `in` is what makes this fair to enforce — it is a length whatever the label in front of it says.

This does **not** generalize to day-a case 8's `hr 65 inches`, which has no reference reading behind it in this set.

### The body-measurement half has an equivalent to B2, and this set cannot host it

Issue #8 asked for both halves — a filled vital and a filled body measurement — because [#7](https://github.com/mshamblin5150-code/clinical-skills/issues/7) widened drift row 4 to reach height, weight and a derived BMI.

**Delivered here:** B1 forces a height, a weight and a derived BMI on all nine cases, so the body measurement is always generated and always declared. B3 then covers it **whenever that BMI lands abnormal.**

**Settled on [#15](https://github.com/mshamblin5150-code/clinical-skills/issues/15), 2026-08-11.** The analogue is a real row and it is **not** contained in B3 — B3 fires only when the value lands abnormal, and the analogue fires on the *normal* branch, where a patient the shorthand calls obese is handed a BMI of 24 and nothing is said about it. There is no abnormal value for B3 to have lost, so B3 passes having tested nothing. The two rows are disjoint, not nested.

**What it needs is an anchor these twelve do not contain.** Cases 5 and 10 carry diabetes, which raises the odds of an obese BMI without documenting one; a row demanding an obese BMI from a documented diabetic would be demanding an invented abnormal finding, which standing rule 2 forbids outright. Nothing in this shift documents obesity itself.

**So the row lives in [obesity-bmi](../obesity-bmi/assertions.md) as O2**, on the two encounters in the whole corpus that write the word and supply no body measurement. This section is now a pointer rather than a gap: day-b's body-measurement half stays **conditional by construction**, and that is the correct answer for this shift rather than a hole in it.

**#15's own diagnosis of why was wrong, and it is worth recording which part.** The ticket held that the row could not be written at all, because any analogue would have to *force an abnormal BMI*. That was true of the **old** B2 and stopped being true when [#14](https://github.com/mshamblin5150-code/clinical-skills/issues/14) rewrote it: B2 no longer forces a value, it forces an account, and an account can be demanded of a normal value without ordering up an abnormal one. The obstacle was never the rule. It was the shift.

**The reference is evidence the gap matters.** The clinician generated a BMI of 37.8 for case 9 unprompted — from a shorthand that documents lymphedema, arthritis in both knees and a hip fracture, none of which requires it — and then addressed it nowhere. **B3 would already have fired on it**, which is the half this set does hold.

### B12 and B13 score which value was chosen, which is what B1 and B3 leave open

B1 forces a filled vital to exist. B3 forces an abnormal one to be worked up. B4 guards the givens. **Not one of them asks which number was written**, and *the value this patient most plausibly had* is the license's actual instruction — so a run could satisfy every FILLED row in this file with nine notes describing the same patient.

**That is not hypothetical, and the defect only exists at the scale of a set.** Run 2 gave every filled male height `5'10"` and every filled female height `5'4"` or `5'5"`; `138/84` appears twice and `138/86` twice; **cases 1 and 5 share an identical `5'10" / 190 lb` at ages 36 and 68.** Twelve independent passes were needed to see it, and each of the twelve notes is defensible read on its own — a 36-year-old man at 5'10" is an ordinary patient. [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67).

**Run 1 did the same thing and run 1 is committed**, which matters because run 2's output is gitignored and a reader cannot check it from the repo. [filled-anchor](../filled-anchor/notes/)'s twelve inputs are run 1 byte for byte:

```bash
python tools/filled_vitals_census.py fixtures/filled-anchor/notes
```

Nine filled heights over **four distinct values**, four of them `5'10"` — including the 17-year-old's. Six of the nine filled pressures **not normal**, on B2's own reading of that word. `tools/test_filled_vitals_census.py` pins both figures, so an edit to those notes voids the argument loudly rather than quietly.

**B12 reaches all twelve and B1's nine is the wrong list for it**, which is worth stating because every other vitals row here uses that list. B1's nine are the cases with no vital line at all. B12's subject is *any* declared-filled value, and the three controls fill some too — case 3's severity is filled, and a case can carry a given pressure and a filled respiratory rate in the same note. **So the row is scoped by what the block declares rather than by which cases the shorthand left empty**, which is B5 and B6's scoping and not B1's.

**They are two rows because they are two failures, and one of them is not a failure of choosing.** B12's subject is a note that chose a number and did not say from what — which one note can be held to, and which is the whole of what a per-case row can reach. B13's subject is two notes agreeing, which no per-case row can see at all. **Neither contains the other**: a run can name an anchor on every line and still hand two patients the same body, and a run can produce twelve distinct bodies while naming nothing.

**B13's fail condition is deliberately the weakest form of the cross-set claim.** The visible pattern is clustering — four heights at one value, six pressures on one side of a line — and *identical* is a much smaller thing to assert. That is on purpose: a row saying *no more than N at one value* needs an N, and nothing grounds one. The corpus says roughly half his transcribed pressures are below 130/80, which is a direction rather than a bar, and nine samples cannot be graded against a proportion. **What cannot be enforced is counted instead**, which is R5.

**A repeat is not automatically a defect and the row still fails on one.** Two patients of the same age and sex with nothing to distinguish them may honestly get the same height, and `clinical-note` forbids inventing a difference to break the pattern — so a failing run is not told to change a number, it is told the set has stopped reasoning per patient. **The remedy is upstream of the value**, in what the note names as its anchor, which is why B12 sits beside this row rather than under it.

**B12 gained a carve-out on 2026-08-15 that no case here can exercise, and it is written into the row anyway.** [#59](https://github.com/mshamblin5150-code/clinical-skills/issues/59)'s ruling bars a filled `0/10` severity from the *no anchor* exit: for a zero, *nothing anchored this* and *I read the complaint and the exam and found no pain source* are the same sentence, so the exit that is a disclosure for a height is an unchecked claim for a zero. **No case here can raise it** — B8 and B14 forbid all three invented severities a zero, and the two zeros in this set are givens. **The row carries it regardless**, because a fixture row that reads as the whole of a rule while silently holding the retired half is worse than one that names a limb it cannot score. **The carve-out changes no verdict**: B12 is unscoreable against the reference permanently, and the case that would exercise it is [#138](https://github.com/mshamblin5150-code/clinical-skills/issues/138).

## CODING — binary, all must pass

A fourth assertion class alongside DRIFT, FILLED and REPORTED, defined in [fixtures/README](../README.md). Binary, on the same terms as the other two.

**It exists because [#19](https://github.com/mshamblin5150-code/clinical-skills/issues/19) put a code on every differential entry.** [SOAP.md](../../skills/clinical-note/SOAP.md) previously carried codes on the preexisting diagnoses and the final diagnosis and none on the differential; it now carries all three, and [drift row 13](../../skills/clinical-note/SKILL.md) is what checks it. That rule fires on **every output this set produces**, which makes it both the most-exercised rule the set can hold and the one that would erode most quietly — a differential with the codes missing reads perfectly well.

**C1 therefore spans all twelve rather than anchoring on one case.** The ticket that opened this class described two rows on case 9, and C2 is the one that needs case 9's particular input. C1 needs nothing particular: every case produces a differential, so scoping it to a single case would test one twelfth of a rule that fires twelve times and leave the other eleven outputs unchecked for free.

| # | Cases | Passes when | Fails when | Reference did |
| --- | --- | --- | --- | --- |
| C1 | all twelve | Every entry in the Assessment's differential carries an ICD-10-CM code. **An entry is one numbered item**, and the count runs to every diagnosis-shaped line in the Assessment rather than stopping at the `Differential:` heading — a measurement of the patient's own body is a diagnosis here, a line of reasoning is not | Any entry carries none. **Two counts that either match or do not** — differential entries, and codes among them. **A differential written as prose fails drift row 23 and leaves this row ungraded**, because a run that chooses its own denominator has not been counted | **Failed on all twelve — not one differential entry carries a code.** Read 2026-08-13 on [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43)'s session. The pattern is the same on every case: the numbered Assessment items carry codes, and the `DDx:` list beneath them is lettered entries with a reason and no code at all. So the question this cell posed — whether any submitted Assessment carries a coded differential, which would make matching it *neither* — is answered **no**, and coding the differential is *better*. |
| C2 | 9 — 44 F | The COVID-19 entry in the Assessment's differential carries a code **other than `U07.1`** | That entry's code **is** `U07.1`. The contact is documented and **no swab was taken**, so that descriptor asserts a disease nothing established | **Read 2026-08-13, and the reference is out of reach of this row twice over.** It carries no differential codes anywhere — C1 above, confirmed rather than assumed — so it has no code on that entry to be wrong. **And case 9's differential has no COVID-19 entry at all**: its three lettered entries are viral URI, allergic rhinitis and otitis media, so there is not even an uncoded entry to point at. Producing `Z20.822 Contact with and (suspected) exposure to COVID-19` here is *better*; producing `U07.1` is *worse* than a note that coded nothing. |

**C1's unit was undefined for as long as it has existed, and the definition above is [#70](https://github.com/mshamblin5150-code/clinical-skills/issues/70)'s ruling rather than a tidy.** The row promised *two counts that either match or do not* and never said what it counted, so the recorded digit turned on an unstated choice **twice** — once on a prose paragraph that argued three diagnoses down inside the differential, and once on a second headed block written after the conclusion. **Both counts under both readings are stated once each, in *What run 2 found* and *What run 3 found* below, and are deliberately not repeated here.** What matters at the row is only that **both readings were defensible on both runs**, which is what a binary row may not permit.

**The clinician ruled on 2026-08-16, against rendered note text rather than a description of it.** The count is **wide** — every diagnosis-shaped line in the Assessment, whatever heading sits above it — because the narrow reading is escapable by moving an uncoded diagnosis one heading down. A **measurement of the patient's own body** is a diagnosis and takes its code, `Body mass index 28.6, in the overweight range - Z68.28`; a **line of reasoning** is not, and belongs in the rationale of the entry it concerns rather than standing on its own line. And the `Name - CODE: rationale` form stopped being permitted and started being **required**, numbered and ranked most likely first, which is `clinical-note` **drift row 23**. The rule is in [SKILL.md](../../skills/clinical-note/SKILL.md) under *The shape of the differential* and in both templates, so the next run does not pick its own reading again.

**C1's *Fails when* cell names a drift row, and that is a reference to the rule rather than to a verdict.** [ADR 0001](../../docs/adr/0001-fixture-asserts-on-named-findings.md) rejects *"the drift-matrix verdicts the skill emits about itself"* as fixture signal, because a run that misses a finding is precisely the run that emits `row 2: PASS`. Nothing here reads a run's row-23 verdict. What C1 says is that a differential written as prose leaves **a reader** with no unit to count, so the row has no two integers to compare — a fact about the output text, checked externally, which is what this ADR asks a row to rest on.

**Neither run's `CODING` digit is restated here, and that is deliberate rather than pending.** #70 put re-running `day-b` and re-scoring either run outside its scope, and both digits stand as recorded — awarded under the row as it read at the time, with the alternate reading named beside them. **What the definition changes is what a *future* run is graded against**, and a run graded under it would also have to clear row 23 first: an unnumbered differential leaves C1's two counts resting on which reader called which line an entry, so `C1 pass` beneath a row-23 failure would be a verdict about nothing. That ordering is stated in [SKILL.md](../../skills/clinical-note/SKILL.md) beside row 23 and is not restated here.

**C2 is scoped to the code that entry carries, not to the string appearing anywhere.** An earlier wording failed when `U07.1` appeared anywhere in the output, and that would have failed a **correct** run: [icd10-cpt](../../skills/icd10-cpt/SKILL.md) step 4 *requires* the refused code be named — `NOT CODED: U07.1  COVID-19` — precisely so the clinician who gets a positive swab back tomorrow knows what it earns. A row that punished the refusal for naming what it refused would have inverted the rule it was written to hold. **Read the differential entry; the worksheet's refusal block is not the note.**

**Its pass and fail are complementary by construction**, which [fixtures/README](../README.md) asks of a binary row. The entry's code either is `U07.1` or it is not, and C1 is what guarantees there is a code to look at — without C1 an entry carrying none would resolve to neither. That is why these two rows ship together rather than C2 alone. Naming `Z20.822` as the expected shape stays commentary in the reference column, where a judgment is allowed; the row itself turns on one string.

**Case 9 is the only case in this set that can host C2.** The rule needs an input documenting a specific organism the encounter never tested for. Case 9 documents a positive COVID contact and orders no swab, which is the same property D6 rests on. Cases 8 and 12 document contacts too and **order the swab** — `covid` on case 8, `covid, strep, flu` on case 12 — so a run over either has a test to hang the diagnosis on and the row would not fire. Case 10 orders testing against no contact at all. That is the same reasoning that left D6 a single row.

**C2 and D6 are not the same row and neither contains the other.** D6 asks whether the Plan **orders the swab** — whether a documented given survived into an action. C2 asks what the Assessment **codes** when it has not. A run can pass D6 and fail C2 by ordering the swab and then coding `U07.1` as though the result were already in, and that is the more likely failure of the two: having written the order, the diagnosis feels established.

**Neither row is testable on a hedge in the input.** Zero of this set's twelve inputs carry a hedge token — verified by `tools/test_corpus_census.py` rather than asserted here — so #19's other half, what the skill does when the *shorthand itself* says `prob viral`, has no anchor in day-b and needed a fixture of its own.

**That fixture is [`hedged-dx`](../hedged-dx/assertions.md), landed 2026-08-15 for [#49](https://github.com/mshamblin5150-code/clinical-skills/issues/49).** Three encounters picked out of `scratch/` — [fixtures/README](../README.md) forbids authoring one — hedging a pneumonia's organism, an osteomyelitis and a mycoplasma against a negative film. **The gap is closed and this set is unchanged**: C1 and C2 still read the output of a differential the *skill* generated, which is a different question from what it does with a hedge the clinician wrote, and neither row moves. What the new set cannot reach either is the `icd10-cpt` half — a note has no `NOT CODED` block — and it says so in its own *Still unresolved*.

## GIVEN — binary, all must pass

This set's fifth section and a new assertion class, defined in [fixtures/README](../README.md) and hosted here first. DRIFT asks what a note did with a **finding** it was given, FILLED asks what it did with a value **nobody supplied**, CODING asks what the codes claim. GIVEN asks the question none of those reach: **did a given survive at all, unchanged.**

| # | Case | Passes when | Fails when | Reference did |
| --- | --- | --- | --- | --- |
| G1 | 12 — 16 F | **All seven orders on the plan line appear in the note as orders.** `covid`, `strep`, `flu`, `ua`, `micro urine`, `urine c/s` and **`spot mono`** — seven, counted. Each named in the Plan or on the results line, each with no result attached, because the source records none | Any of the seven is absent. **And a note that replaces one with a statement that it was not done fails harder** — `No monospot sent on those findings` in the Assessment is not a milder miss than silence, it is a false claim about the encounter. An objection to an order passes only where the order itself is still there | **Kept all seven, in two places.** The `Tests` line lists `COVID, strep, flu, UA/micro/urine C&S, spot mono`, and the differential's mononucleosis entry reads `spot mono ordered`. **There is nothing to beat on this row and everything to lose** — matching it is *neither*, and dropping one is the set's clearest available *worse*. |

### Why this is not a DRIFT row, and why widening DRIFT would have been the expensive fix

DRIFT is defined at the top of this file as *a finding the shorthand documented that a note can drop between the Objective and the Assessment*, and [fixtures/README](../README.md) states the same thing as *a documented abnormal that reached the Objective and stopped there*. **An order is not a finding and not an abnormal.** It is not something observed about the patient; it is something the clinician did.

Widening DRIFT to admit it would have been one word in a definition and a retroactive change to what `DRIFT 10/10` and `DRIFT 7/7` mean across two sets. **A class name is a denominator other files quote.** Appending a class costs a section; editing one costs every score already recorded under it.

### It is not a FILLED row either, and B4 is the reason that needed checking

B4 — *every **given** vital appears in the note unchanged* — has GIVEN's subject and sits in FILLED, because it is there as the control on the filled-vital license. **B7 is the same shape** for a given severity. Both stay where they are and neither is renumbered: this file's own convention is **append, never insert**, and moving a row silently redirects every citation to it. What the two of them show is that the question was already being asked in two places without a class to ask it in, and only ever about values.

**G1 is what the pattern looks like once it is not about a value.** An order has no number to be altered, so B4's failure shape — *a recorded value is replaced, rounded, or duplicated by a generated one* — cannot describe it. The only thing that can happen to an order is that it survives or it does not.

### The run-2 observation, which is evidence and not a score

Run 2's case 12 carried six of the seven, omitted the monospot, and wrote `No monospot sent on those findings` into the Assessment. It then listed `Not sending a monospot` under `FILLED·proposed`, named six orders in GAPS, and cited *no monospot* in its own drift row 15 verdict as a workup withheld on given findings — **so the note graded the erasure as a pass, four times over.** Run 1 carried all seven, on the same input, and its output is committed at [filled-anchor/notes/case-12.md](../filled-anchor/notes/case-12.md).

**This row is nonetheless unscored, and the reason is this file's own convention rather than modesty.** `clinical-note` gained drift row 18 with G1, and those twelve notes predate it — *a number that belongs to no commit is worse than a gap that names itself*. The re-run is [#75](https://github.com/mshamblin5150-code/clinical-skills/issues/75).

**The convention is applied here at its weakest**, and that is worth saying plainly rather than hiding behind it. Row 18 is the second row whose prohibition already existed — *a given passes through unchanged* is the first sentence of the tier system, and row 11 has forbidden dropping a given medication throughout. So unlike B5 through B8, run 2 did not fail a rule written after it; it failed a rule nothing had ever been made to walk. The verdict is deferred to run 3 because a score is a score, **not because the behavior is in any doubt.** [#66](https://github.com/mshamblin5150-code/clinical-skills/issues/66).

### One row, and the set can host more

Every one of the twelve carries a plan line and every one of them orders something, so G1's shape is available across the whole set. It is written on case 12 alone because case 12 is where a run was **observed** to fail it, and this file does not author rows against defects nobody has seen — that is [ADR 0001](../../docs/adr/0001-fixture-asserts-on-named-findings.md)'s asserts-on-named-findings discipline applied to a class rather than to a row. Case 7's plan line is the obvious second candidate: it is the longest in the set, and long enough that `UA` and `micro urine` being one order or two changes the count, which is a question a second row would have to answer before it could be binary.

## REPORTED — counted, not enforced

Five rows. R1 came out of the reference read, as a difference of the fourth class; **R2, R3 and R4 came out of [#29](https://github.com/mshamblin5150-code/clinical-skills/issues/29)**, which reversed most of what R1 originally claimed and left this set needing rows that say what a *passing* social history looks like rather than only what a failing one does. **R5 came out of [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67)** and is counted for a third reason again — see below. Counted rather than enforced on day-a's terms: *"a bar is only worth having if it was set deliberately"*.

**Run 1 scored `0/1`, and that score is now `0 of 1 row out of 5`.** Case 1 asserted an allergy status against a shorthand silent on allergies, plus an education level, a marital status and an occupational detail; case 2 asserted the allergy status again, plus an occupation and an education level.

**Two of those four assertions are correct under #29 and two are not**, which is the whole reason the row was rewritten rather than deleted. The allergy status is what [SOAP.md](../../skills/clinical-note/SOAP.md) instructs and what the corpus supports; the marital status `clinical-note` requires be inferred from age. The **occupation and the education level are grounded in nothing**, so the narrowed R1 still fails run 1 — the miss is real and it survives the reversal.

**Run 2 scored it `0/1` as well, and went backwards on the one case run 1 got right.** Case 1 asserts `NKDA` against a shorthand silent on allergies, plus an education level, a marital status, a diet and a sleep pattern. Case 2 asserts `NKDA` plus `Retired`, an education level, a marital status, a diet, an exercise habit and a sleep duration — **and writes `Environmental: seasonal allergies - rhinorrhea, sneezing, itchy watery eyes` against a shorthand carrying the bare words `seasonal allergies` and no reaction.** Run 1 wrote `seasonal allergies (stated), reaction pattern not documented`, which that run was scored as getting right. Case 1's inferred antacid rides on a documented `heartburn ocasionally` and is arguably exempt with case 2's antihypertensive.

### Run 2 was scored against the previous R1, and #29 changes what it says

**Both were made against `e4cd503`** — run 2 names that commit and [#29](https://github.com/mshamblin5150-code/clinical-skills/issues/29) branched from it. So run 2's `REPORTED 0/1` is a real score against a row whose text the same base commit was in the act of replacing, and three of its findings move:

| Run 2 wrote | Then | Now |
| --- | --- | --- |
| `NKDA` on cases 1 and 2 | a defect | **correct** — the template instructs it and the corpus supports it |
| `seasonal allergies - rhinorrhea, sneezing, itchy watery eyes` | *"the reference's own defect, reproduced"* | **correct** — the template asks for a reaction and those are what that allergy does |
| `reaction pattern not documented` (run 1) | *"the passing form"* | **banned** — a hedge, and drift row 12 already forbade it |
| `Retired` on a 71-year-old | an ungrounded occupation | **correct** — age grounds it, as age grounds `Married` |
| education, diet, sleep, exercise | ungrounded | **ungrounded still** |

**The row still fails on both cases, and that is the only reason the number survives the rewrite.** What fails is no longer the allergy status or the occupation — it is the education level and the lifestyle slots, on both cases. **`REPORTED 0/1` is therefore right by accident**, and a re-score is owed rather than a correction: the same digit for four different reasons is not the same measurement. [#75](https://github.com/mshamblin5150-code/clinical-skills/issues/75).

**Run 2 also falsified this row's first narrowing, which is the useful part.** R1's claim was written as *"occupation and education are the two this set catches"* — an inference from run 1's two cases, made without run 2's output in view. Run 2 asserts a diet on both cases, a sleep pattern on both, and an exercise habit on one, and grounds its occupation properly. **So the row names a test and not a list**, which is what it should have done at the start: enumerating the slots a set catches is a claim about runs nobody has made yet.

**Two labels are in play here and they are not the same one.** The `Reference did` cell grades the *reference*, and it reads ***out of reach***: he asserted these having been in the room, which is what puts his version beyond the skill. A run's `0/1` grades the *skill*, and it is ***worse***: it was not in the room and asserted them anyway. #29 disturbs neither.

**The old rationale for keeping this counted is gone and a different one holds.** It used to read that R1 *"turns on phrasing"* — scoring whether a declaration reads as not-reported — which [fixtures/README](../README.md) disqualifies from being binary. **The narrowed row does not turn on phrasing at all**: it asks whether an asserted social detail is grounded in something the shorthand contains, which is a question about provenance and resolves the same way for any two runs. So all four rows could now be enforced, and *R2 through R4 are counted rather than binary* below says why they are not.

**R2 through R4 are ungraded from either run**, on this file's standing rule: those notes were written before drift row 17 existed, so a score from them would belong to neither commit. `REPORTED 0/1` is a value for one row, and **`0/4` is not what it means.**

**Run 3 graded all four and scored `1/4`.** R1 fails on case 1's education slot alone and passes case 2 outright; R2 fails on cases 7 and 11, both in the allergy slot and both on a given allergen with no documented reaction; R3 passes twelve for twelve; R4 fails on cases 5 and 9, of five firings with four cases vacuous. So the denominator is now real and the three failures are in three different places — **R1's is a slot filled from nothing, R2's is a slot the rule leaves no way to fill, R4's is a disclosure pointed at the wrong drugs.** The first and third are the run's defects and the second is the rule's.

**R4's digit was `2/4` until 2026-08-14 and this is a re-score, not a re-run.** [#95](https://github.com/mshamblin5150-code/clinical-skills/issues/95) settled that the row asks for correct enumeration rather than a line's mere existence, which turns cases 5 and 9 from imprecise passes into misses. **The verdict is derived from the two lines quoted in that ticket and from this file's own record of them, not from a fresh read of the output** — and `scratch/day-b-run-3/`, which the scorecard above cites, is not on this machine. **Case 1 is the fifth firing and neither record says what it did**, so the row's verdict rests on cases 5, 6, 8 and 9. Two failures are enough to fail a row and one more cannot unfail it, so the digit holds either way; what is missing is the detail, not the result.

| # | Cases | Claim | Reference did |
| --- | --- | --- | --- |
| R1 | 1, 2 | **No social detail is asserted that the shorthand cannot ground.** A slot filled from a given passes — `at work` grounds `Employed`, an age of 71 grounds `Retired`, an age grounds `Married`. A slot filled from nothing fails, whichever slot it is: `Works manual labor` from `at work`, an education level, a diet, an exercise habit, a sleep pattern | Asserted both. Case 1: `Social: Works manual labor` against a shorthand saying only that the injury happened `at work`, plus an education level. Case 2: an occupation and an education level, neither grounded in anything the shorthand carries. ***Out of reach*** — right in his note, forbidden in the skill's. |
| R2 | all twelve | Every social and allergy slot the branch template enumerates carries a value, and **none of them is a hedge** — no `not documented`, `not reported this visit`, `status unknown` or blank clause | **Failed on completeness, and the hedge half never fires.** The reference enumerates no slots at all: social and allergy content is written into the HPI prose where it is written, and **cases 6, 9, 10 and 12 carry neither anywhere in the note.** **Not one hedge appears in the twelve** — no `not documented`, no `not reported this visit`, no `status unknown`, no blank clause. He omits what he does not have rather than declaring it missing, which is drift row 12's ban reached from the other side. Enumerating every slot with a value is *better*; the phrasing this row forbids is one he never writes. |
| R3 | all twelve | Every **filled** slot is declared in `FILLED·asserted` carrying its value — `SH tobacco Non-smoker filled`, not `social history filled` | **Unscoreable against the reference, permanently.** A submitted note carries no tier block, so it declares nothing and cannot pass or fail. Same position as B9. |
| R4 | 1, 5, 6, 7, 8, 9, 10, 11, 12 | Where a **proposed** drug rests on an inferred allergy status, that `FILLED·asserted` line names the dependency **and enumerates it correctly**: every proposed drug resting on the inference, and **no drug the shorthand already ordered**. **Naming a given fails as surely as omitting a proposed one** — the first asks the clinician to re-confirm his own prescription, which is [#32](https://github.com/mshamblin5150-code/clinical-skills/issues/32)'s flag-on-every-encounter failure arriving in the tier block instead of the note; the second leaves the drug the skill chose standing silently on the value the skill invented, which is the harm the rule exists for. **A continuation is a given**: `continue abx` for a drug the patient has taken two days owes no line, because an inferred allergy status cannot underwrite a drug already tolerated | Unscoreable, for R3's reason. |
| R5 | **the set** | **Three numbers, recorded and not graded.** Over the filled values the set produced: how many **distinct heights**, how many **distinct pressures**, and how many pressures land **not normal** — systolic 130 or above, or diastolic 80 or above, which is B2's own reading of the word. All three printed by `python tools/filled_vitals_census.py <the run directory>` | **Unreadable against the reference, and the corpus answers the question it was asked for.** A submitted note does not say which of its values were filled, so no share of *his* filled pressures can be computed. The number a run is drifting from is the shorthand's, and `clinical-note` already carries it: **of 249 transcribed pressures, half are below 130/80.** |

### R1 was two claims and only one of them survived #29

The row previously read *"No social, allergy or medication detail is asserted where the shorthand supplies none. Absent or not-reported phrasing is the passing form"*, and it caught three things. [Issue #29](https://github.com/mshamblin5150-code/clinical-skills/issues/29) struck the first two and left the third.

**The allergy half is struck, and it was failing the skill for obeying its own template.** [SOAP.md](../../skills/clinical-note/SOAP.md) has always written `Allergies (reaction): <allergen - reaction; NKDA if none>` — the branch template **instructs** `NKDA` where the shorthand is silent, and this row scored it as a defect. That is precisely the objection the note below already made about case 2's inferred antihypertensive, arriving a second time in the same row and going unnoticed: *a row that failed it would be failing the skill for obeying its own instructions.*

**The corpus agrees with the template.** Over the 31 committed fixture inputs, **16 carry an allergy clause and 11 of the 16 say `NKDA`** — he fills that box whether or not there is anything in it, so silence is a transcription gap and `NKDA` is the value most plausibly held rather than a bland pick. Contrast the tobacco slot: 15 cases write it and **14 of 15 state a positive history**, so silence there is a real absence and a positive fill would be an invented abnormal. That asymmetry is why [day-a](../day-a/assertions.md) R14 survived intact while this half died. `tools/test_corpus_census.py::SocialSlotsSplitTwoWays` pins both splits per case.

**Case 2's reaction is struck with it.** `Allergies: Seasonal-itching/sneezing` against a bare `seasonal allergies` adds the reaction, and the template asks for one — `allergen - reaction`. Itching and sneezing are what seasonal allergies do, so the reaction is grounded in the given rather than invented beside it, which is the test the surviving half of this row now applies. **Run 2 wrote the same thing and was scored down for it**; that finding is reversed above rather than quietly dropped, because a run graded against a row that has since changed is owed the reversal in writing.

**The not-reported phrasing is struck as the *passing form*.** `Allergies (reaction): Not documented this visit` is a sentence that defends the note rather than reporting on the patient, which **drift row 12 has banned since [#28](https://github.com/mshamblin5150-code/clinical-skills/issues/28)**. This row was rewarding a row-12 violation. R2 is now what forbids it.

**What survives is the ungrounded biographical claim**, and it is the half the clinician's ruling never reached. `Works manual labor` is not an abnormal finding and not a sentence about this skill's process, so neither row 1 nor row 12 catches it — it is simply a fact about a patient's life that nothing in the encounter supplied. `at work` grounds `Employed` and stops there: nothing in a thumb laceration says manual labor rather than a paper cutter. That is [SKILL.md](../../skills/clinical-note/SKILL.md)'s own *grounded in what it contains, not invented beside it*, which was in the file the whole time and applied to the exam and the medication list but never to the `SH:` line. Drift row 17 is where it now gets walked.

**Run 1's `0/1` does not flip, and that is worth stating.** Run 1 asserted, on case 1, an allergy status plus an education level, a marital status and an occupational detail. Under #29 the allergy status is correct and the marital status is licensed outright — `clinical-note` instructs that it be inferred from age. **The education level and the occupation are still ungrounded**, so the narrowed row still fails that run. A reversal that retroactively awarded a pass would have been a reason to distrust the rewrite.

**One thing day-a's R14 has that this row does not.** R14 records *"Recalled from the encounters, not invented, per the clinician on 2026-08-11"* — he was asked, about those four. **He has not been asked about these**, so *out of reach* is carried over by analogy rather than confirmed. It does not change what the row demands. It changes only the label, and *worse* is the alternative.

**Case 1 is the row's own counter-example, in the same note.** It writes `Meds: None reported`, `No DM history reported` and `no PAD reported` — grounded absences, three times — and then `Works manual labor`. The distinction is available to him; it is not applied evenly.

### R2 through R4 are counted rather than binary, and they would qualify

Each resolves to a value or its absence, which is [fixtures/README](../README.md)'s stated criterion — count the template's slots, count the ones carrying a value, count the ones the block declares with a value, and read whether a withheld dependency is named. None of that moves with wording, so all three could be enforced.

**They are counted anyway, for two reasons.** Neither is about the rows' quality. The set already fails its binary bar at `FILLED 3/4` and has twelve rows never scored at all, so three more binary rows would widen a gap [#55](https://github.com/mshamblin5150-code/clinical-skills/issues/55) exists to close. And [#29](https://github.com/mshamblin5150-code/clinical-skills/issues/29) is one ruling landing in two sets: enforcing this set's half while day-a's waits would leave the two files holding the same rule to different standards. **The rows that ruling touches are five** — R1, R2, R3 and R4 here, and [day-a](../day-a/assertions.md) R14 — **and they are promoted together or not at all**, after run 2 has scored them once.

**They are R-numbered rather than B-numbered, and the reason is a genuine collision rather than a tidy rule.** By subject these belong to FILLED: [fixtures/README](../README.md) defines that class as *"what the skill does with a value the shorthand never supplied"*, which is exactly a filled slot value. By bar they belong to REPORTED, because the clinician ruled them counted. **README defines FILLED as binary in the same breath as defining its subject**, so a counted FILLED row is not a thing this repo has — `B10` would contradict the heading above it and `FILLED 3/7` would report a bar that was never set.

**The prefix follows the bar, because the bar is what a scorecard line reports.** `FILLED 3/4` and `REPORTED 0/4` are read off the `Last run` column, and a reader who cannot tell which rows a denominator covers cannot use it — which is the failure the row-count correction in [fixtures/README](../README.md) was made to stop. Subject is recoverable from the row text; the bar is not. **What this costs is that the class names no longer partition by subject alone**, and that cost is recorded rather than hidden: README now says which rows are counted for want of a run and which for turning on phrasing, because only the first kind is promotable.

### R5 is counted for a third reason, and it is not promotable either

R2 through R4 are counted **for want of a run** and are promotable once they have one. R1 was counted for turning on phrasing and no longer is. **R5 is neither**, and saying which it is matters because [fixtures/README](../README.md) offers only the two.

**Its two figures do not move with wording** — a distinct-value count and a count above a line are integers a machine prints. So the criterion for enforcing it is met and it is still not enforced, because **there is no bar to set.** The corpus says roughly half of 249 transcribed pressures are below 130/80. That is a direction, and turning it into a pass condition over **nine** samples means choosing a number: five of nine? seven? A row saying *no more than six* would be a threshold invented by whoever wrote the row, and a run failing it would be failing an opinion.

**So the enforceable part was written as B13 and the rest is recorded.** *Identical* needs no threshold, which is exactly why that limb is the binary one. Everything else about the distribution is a count that a reader compares to the corpus and rules on — and **a count nobody has to pass is still the thing that found this defect**: run 2's nine heights over a handful of values is what [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67) is, and no binary row in this file would have caught it.

**A third reason to count a row is a change to [fixtures/README](../README.md), not a local exception**, and it is made there rather than only here.

**Case 2's `Meds: Unspecified antihypertensive` is deliberately not counted here.** The shorthand carries no meds line, so on its face it is the same defect — but [`clinical-note`](../../skills/clinical-note/SKILL.md) explicitly instructs the skill to *"infer the likely regimen"* in exactly that situation, and `Unspecified` is the hedge doing its job. A row that failed it would be failing the skill for obeying its own instructions. **The instruction survived [#23](https://github.com/mshamblin5150-code/clinical-skills/issues/23)** and came out of it load-bearing: an inferred regimen is now one of the four things that can account for a normal filled pressure. So this stays uncounted for a stronger reason than it started with.

## What the reference surfaced that this set does not assert

Three defects in the submitted record are real and are **out of scope for a fixture whose inputs carry no dates and no portal fields.** They are recorded in `scratch/day-b-reference/README.md` so they are not lost:

- **Case 9's Visit Date is the day the note was typed, 78 days after the encounter.** The form's own date field carries the encounter date, so the two halves of the record disagree with each other. A date-range search for the shift therefore returns eleven of the twelve, and case 9 had to be found by patient creation order instead. (The dates themselves stay out of this file for the reason at the top of it.)
- **The shift had thirteen encounters, not twelve.** A thirteenth on the same date is not in the day file at all. A day file's note count is not a census of the shift.
- **Case 12's age is recorded as 18 against a shorthand that reads `16 yo F.`**, which put a pediatric hour into `Adult (18 – 60)`.

The inputs cannot test any of these: they carry no visit date, no portal entry fields, and the age is given correctly in the shorthand. Testing them needs a different kind of fixture than this one.

## Still unresolved

- **B3 passes on run 2 and [#47](https://github.com/mshamblin5150-code/clinical-skills/issues/47) is not fixed.** The two are separable and run 2 is what separated them. Case 5's filled BMI is now named in the Assessment and the Plan, so the row clears — but #47's defect is the **self-report over-claiming the Plan**, and run 2 committed it again on **case 10**: the Assessment says the BMI of 28.0 *"is addressed in the plan rather than merely recorded"*, drift matrix row 4 says it is addressed *"in the Plan through nutrition, glycemic and lipid management"*, and that Plan carries no weight, nutrition or activity line at all — its one dietary item is magnesium-rich foods, which answers the magnesium of 1.6.

  B3 survives because it is a disjunction: the Assessment names the value, so nothing stopped at the Objective. **Nothing about #47 survives.** It is run 1's case-5 shape one notch milder — there the matrix claimed a pass on weight counseling that did not exist *and* the value was named nowhere; here only the Plan follow-through is invented. Two runs, two cases, one behavior. Still **not resolved by editing this row**, and still filed with peds-bp's identical P5 miss.

  **Run 3 makes it three runs and three cases**, on **case 7**: matrix row 4 says the BMI of 28.3 *"is addressed with counseling and confirmed measurements at follow-up"*, and that Plan carries no weight, nutrition or activity line anywhere. The counseling sits in the Assessment and in `FILLED·proposed`, and the Follow up line confirms the measurements without counseling anything. B3 passes on case 7 for the same disjunction reason it passed on case 10. **The behavior is now the most reliably reproducible thing this set measures**, and three separate cases have carried it under three different commits.

  **What run 3 adds is the contrast.** Six other cases in the same run carry a real Plan line for the same value — case 4's walking program, case 5's 150 minutes per week, case 8's weight and dietary counseling, case 9's 5-to-10% target, case 10's diet and activity counseling, case 11's portion-size review. So the defect is not the skill being unable to write the line. It is one note claiming a line it did not write.

  **Ruled 2026-08-15, and the rows are B15 and B16.** The clinician settled both halves: *addressed* narrows to the Plan carrying education, an order or treatment, or the Assessment attributing the value to something the Plan is already treating — a recheck of the value is not an address, and neither is a promise of a Plan item that does not exist. The **mechanism** is a count rather than a judgment call: every `FILLED·proposed` item is named where it landed, and one that landed nowhere was dropped. `clinical-note` gained the narrowed drift row 4 and **drift row 21** with it. **B3 is still not edited and still passes case 7** — it is the weaker precondition now, and that is deliberate, because editing it would redate three recorded scores. **What stays open is a run**: both rows are unscored, and the behavior they catch is known before anyone scores it.
- **Run 3 happened and twenty-four of the thirty-one rows have a value.** [#75](https://github.com/mshamblin5150-code/clinical-skills/issues/75) ran on `b65b9f6` over every row then existing: `DRIFT 7/7` · `FILLED 10/11` · `CODING 2/2` · `REPORTED 1/4`, and **it fails on B6, R1, R2 and R4** — R4 since #95's ruling of 2026-08-14, which is a re-score rather than a re-reading. R1's re-score is done and the row still fails, on **one slot of one case** rather than the three-and-four run 2 recorded. What stays open is what a *failing* run establishes, which is as narrow as what a passing one did: **B6 is one case of twelve**, and a row that run 2 passed. One run in either direction is a value, not a property.

  **Every run of this set has now been overtaken by something, and run 3 was overtaken four times.** #75 asked for twenty-two rows against a set holding twenty-four, having counted from run 2's denominator rather than from the tables; the two extra rows were run anyway. Then [#66](https://github.com/mshamblin5150-code/clinical-skills/issues/66) landed **G1** while the notes were being written, and that one could not be absorbed — it is unscored, on the rule that a row existing because the skill changed cannot be graded from notes predating the change. Then [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67) added **B12, B13 and R5** before the record could be merged. **Then [#42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) added B14** on 2026-08-13, and it is ungraded for the same reason G1 is — `clinical-note`'s severity rule changed with it, so twelve notes written before the change would measure neither commit. **Then [#47](https://github.com/mshamblin5150-code/clinical-skills/issues/47) added B15 and B16** on 2026-08-15, from this run's own case 7, and moved that file a fourth time. That makes the seven rows without a value G1, B12, B13, R5, B14, B15 and B16, and the next run is where all seven get one. **The file that exists to stop a stale denominator produced one, and then the run acquired five more.**
- **R4's reading is settled and two things about it are not.** [#95](https://github.com/mshamblin5150-code/clinical-skills/issues/95) ruled on 2026-08-14: the row asks for **correct enumeration** rather than a line's mere existence, **naming a given fails** as surely as omitting a proposed drug, and **a continuation is a given** — so cases 5 and 9 are misses and `REPORTED` re-scores to `1/4`. That was the right order of operations, the one [#79](https://github.com/mshamblin5150-code/clinical-skills/issues/79) needed: **that is the bar, this was the meaning**, and promoting the row first would have frozen whichever reading happened to be used. What stays open:

  **The row still rests on five firings out of nine**, so it is a partial test whatever the reading — cases 7, 10, 11 and 12 carry a given allergy status and the row is vacuous on all four. [fixtures/README](../README.md)'s standard applies to the enumeration bar exactly as it applied to the old one: *"A row the reference never approached has not been tested, and a set that reports it as a pass is flattering itself."*

  **Case 1 is the fifth firing and no record says what it did.** #95 accounts for 6 and 8 as right and 5 and 9 as inverted, and this file did the same; neither reaches case 1. The verdict does not turn on it — two misses fail the row and a fifth case cannot unfail it — but the run record is incomplete on a row that has just been re-scored, and **`scratch/day-b-run-3/` is not on this machine to settle it from.**
- **R2 has no passing form for a given allergen with no documented reaction**, and run 3 is where that surfaced. Cases 7 and 11 each carry stated allergens and no reaction; `NKDA` is false, the allergen may not be dropped, the reaction cannot be grounded and drift row 12 forbids the hedge. Both notes wrote `reaction not documented` and both fail R2 for it. **The gap is in the rule rather than in the run** — `clinical-note` settles the silent allergy slot with a count and settles a grounded reaction with #29's `Seasonal-itching/sneezing`, and settles this case with nothing. **Not fixed by editing this row**: R2 is correct as written and the run failed it fairly. [#94](https://github.com/mshamblin5150-code/clinical-skills/issues/94).

  **It is not day-b's gap either.** Over the 31 committed fixture inputs, **16 carry an allergy clause, 11 say `NKDA`, 5 name an allergen and none of the 5 names a reaction** — so the template's own `allergen - reaction` shape is satisfiable from the shorthand only where the answer is `NKDA`. Three of the five name `seasonal allergies` alone, which #29 already settled. **The two with no settled answer are the two drug-allergen cases, and they are the two that failed.** The other three sit in `day-a` and `peds-bp`, where no row yet asks the question.
- **Run 2 happened and nineteen of the thirty-one rows have a value.** `DRIFT 7/7` · `FILLED 9/9` · `CODING 2/2` · `REPORTED 0/1`, on `e4cd503`, 2026-08-12. What is unresolved is what a passing run does and does not establish: it is one value over nineteen rows, not a property of the skill, and the three bullets around this one are each a place where the number says less than it appears to. What both runs now establish is the thing [ADR 0001](../../docs/adr/0001-fixture-asserts-on-named-findings.md) argues for — the assertions are checkable against real output by someone who did not produce it, they failed a run whose own matrix said it passed, and on run 2 they passed a run whose own matrix was still wrong about a Plan.
- **B2's second exit has still never been exercised by a run of the skill, and it is now two runs rather than one.** The **reference** reached it and failed it — case 8's `124/78` with the hypertension appearing only as `HTN I10` in a pre-existing list — so the exit is not unreachable in principle. It is the *skill* that has never landed there. [#26](https://github.com/mshamblin5150-code/clinical-skills/issues/26) expected run 1 to fail B2, as the visible cost of `clinical-note` and this row contradicting each other. It passed, and structurally rather than luckily: **B2's first exit is an abnormal pressure, and the rule as it then stood produced exactly that** — case 8 filled at 148/92 and case 9 at 146/90, both above on both limbs — so neither case reached the second exit and the clause the two documents fought over was never tested.

  **[#23](https://github.com/mshamblin5150-code/clinical-skills/issues/23) has since been settled in B2's favor.** The rule no longer orders up a hypertensive pressure, so a filled pressure for cases 8 and 9 may now legitimately land normal — which would route them to the **second** exit, the one asking whether the Assessment calls the hypertension controlled or treated.

  **Run 2 was that chance and did not take it.** Both cases filled at **138/86**: closer to the line than run 1's 148/92 and 146/90, still above it on both limbs, still the first exit. So the retired rule is not what was producing the abnormal — the skill produces one anyway. **Read B2 as untested rather than as a baseline**, now against two runs instead of one, and note that the thing to watch is no longer *whether* run 3 routes differently but *why* two runs under two different rules have not. [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67) is the likeliest answer: if the filled pressures are drawn from a template rather than reasoned, they will keep landing just over the line whatever the rule says.

  **Run 3 did not take it either, and it answered the *why*.** Case 8 filled at **148/92** and case 9 at **138/86** — first exit both times, a third consecutive run. What is new is that these are not new numbers:

  | Run | Case 8 | Case 9 |
  | --- | --- | --- |
  | 1 | 148/92 | 146/90 |
  | 2 | 138/86 | 138/86 |
  | **3** | **148/92** | **138/86** |

  **Run 3's case 8 reproduces run 1's value exactly and its case 9 reproduces run 2's exactly**, from twelve independent passes that could not see each other or the earlier runs. A reasoned pressure would not land on the same two readings three runs running. **That is [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67) demonstrated rather than suspected**, and it is why B2's second exit stays unreachable: the draw does not produce a normal number for these two patients. The heights say it again — every filled male height across the nine is `5'10"`, and the female heights are `5'4"`, `5'4"`, `5'5"` and `5'6"`. **B2 is untested against three runs now, and the reason is no longer a mystery to be watched.**

  **#67 has since been measured and acted on, and B2 is where the payoff would show.** Six of the nine filled pressures in the committed run record land not-normal, which R5 now counts; `clinical-note` gained *Which value was chosen is the instruction* and drift row 19, requiring each filled pressure's line to name what it was reasoned from. **If that is what was producing the abnormal, run 3 is the first run whose hypertensives can reach B2's second exit.** If they still do not, the explanation is not the template and this bullet has to be rewritten again — which is worth stating in advance, because a fix that lands beside an unchanged number is easy to credit anyway.
- **B7's pass and fail are not complementary, and the reference is what exposed it.** *Fails when* names a given score rounded, moved or replaced; the reference lost three by **dropping** them, which is none of those, so the verdict has to come off *Passes when*. [fixtures/README](../README.md) asks complementarity of a binary row and this one does not have it. Nothing goes unscored — B5 fails a dropped element independently — so the row is under-specified rather than the set blind, and the fix is a limb on *Fails when* rather than a new row. Not done here because rewriting a row's text in the same change that scores it against the reference is how a set stops being able to say what it tested. **B4 is the vitals analogue and has the same gap** — its *Fails when* names replacement, rounding and duplication and no limb for an omitted given — which no case has exercised because the reference filled every box. [#111](https://github.com/mshamblin5150-code/clinical-skills/issues/111), `grilling`: whether a dropped score should fail B7 as well as B5, or be deferred to B5 outright, is the open decision.
- **Cases 6 and 12 are 17 and 16, and [issue #11](https://github.com/mshamblin5150-code/clinical-skills/issues/11) turned out not to reach them.** That issue asked whether a filled pediatric vital should be filled at all, and pointed here for the fixture. The corpus answered the boundary question first: measured 2026-08-11, a blood pressure going missing from a vital line that was otherwise written happens **only under 6** — every band from 9 up produces not one instance, and a 16-year-old is transcribed exactly like an adult. So these two are adolescent only in the sense that their filled values must suit their age, which B1 already demands. The ruling — filled, no exception — is fixtured in [peds-bp](../peds-bp/assertions.md), whose cases are young enough to test it.

  What is genuinely untested here is **B2's** analogue, not B1's: B2 reaches only cases 8 and 9 because only those two document a condition a normal value would owe an account of — *not* because a normal value is implausible there, which is the rationale the row was rewritten to drop, and neither of these adolescents documents one either way. (An earlier version of this bullet named B1 for that; B1's case list has covered 6 and 12 all along.)
- **Case 3's severity was the OLDCARTS boundary. Resolved 2026-08-13, and B14 is the row.** [#42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) ruled that the scale scores **pain** whatever the complaint is, that the words after the number name the complaint rather than what is scored, and that a complaint which does not hurt takes its number from a pain source in the exam — 0/10 only where there is none. **The bullet predicted the shape of the answer and got the argument wrong**, which is worth leaving on the record: it framed the choice as `0/10` against a scale that grades the itching, and the ruling picked the second value by way of the first argument. `clinical-note`'s sentence was corrected rather than confirmed. The prediction that the answer belongs in the skill file and this set together held.
- **The social-slot split rests on 31 fixture cases and not on the corpus.** `tools/corpus_census.py` gained the two extractors on [#29](https://github.com/mshamblin5150-code/clinical-skills/issues/29) and **has not been run against `scratch/` since** — the figures quoted for R1, R2 and day-a's R14 are a floor over the committed inputs. 14 of 15 tobacco clauses positive against 11 of 16 allergy clauses saying `NKDA` is a direction a wider count is unlikely to reverse, and a direction is what the rows turn on; the numbers themselves should not be quoted as the corpus until someone with `scratch/` runs it.
- **R3 and R4 can never have a reference verdict**, and R2 now has one. R3 and R4 are in B9's permanent position: a submitted note carries no tier block, so it cannot declare a filled value or name a dependency, and no amount of reading will change that. R2 was the readable one and [#43](https://github.com/mshamblin5150-code/clinical-skills/issues/43) read it.
- **B9's pain-score limb rested on a ruling nobody made. Ruled 2026-08-15, and the shipped reading stands.** [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) put the OLDCARTS severity inside drift row 15's class and never asked what that does about row 4, whose normal range for the scale is one value wide. The pass implementing it ruled that a filled `0/10` is not a discharge — no obligation arose — and shipped that; [#59](https://github.com/mshamblin5150-code/clinical-skills/issues/59) filed it rather than leaving it buried in a merged diff, and the clinician has now ratified it. The rejected alternative was that generating a zero into a silent box is exactly a filled value withholding a treatment, which would have made such a note owe the Plan an account the way drift row 14 does.

  **It was ruled without the case this bullet said it needed, and that is the part worth keeping.** **B8 covers the two cases where it would bite hardest**, cases 6 and 9, and this bullet used to say that what actually turned on the answer was **case 3**. [#42](https://github.com/mshamblin5150-code/clinical-skills/issues/42) scored her above zero and B14 now forbids her a zero outright, so the last case in this set where a run could legitimately write a filled `0/10` is gone. What #59 turned on at the end was the patient that ruling created — a complaint that does not hurt in an encounter with **no pain source in it**, which reasons to zero honestly — and **none of these twelve is one**. The clinician ruled against the question rather than against a note, which is weaker than how every rule around it was settled. The case is still owed, [fixtures/README](../README.md) still forbids authoring one, and it still wants an encounter picked out of `scratch/` the way [#65](https://github.com/mshamblin5150-code/clinical-skills/issues/65) did. **#65 has since been closed that way**, which makes it a worked precedent rather than a sibling in the same position: a filter published as a pool, the pool read case by case, and the pick declared a judgment in the set's own README. **[#138](https://github.com/mshamblin5150-code/clinical-skills/issues/138) can take the same route**, and its own comments say so rather than this paragraph inferring it — `corpus_census.py` already carries `PAIN_SCORE` and `has_pain_score` and already reports `no score written` corpus-wide, so a shortlist of encounters with no written score can be emitted as `(file, note)` coordinates inside the counts-only contract. **What is still the clinician's is authorizing the read**, which is narrower than what that ticket's label used to rest on.

  **The ruling arrived with a second one, and this set cannot score that either.** `clinical-note` now bars a filled `0/10` from drift row 19's *no anchor* exit: for a zero, *nothing anchored this* and *I looked for a pain source and found none* are the same sentence, so the zero's line has to name the search. **That is the only check on a filled zero that happens after the value is chosen** — B8, B14 and `clinical-note`'s *A reasoned 0/10 is not the default* all sit upstream in the choosing, and row 15 asks the Plan for nothing. B12 is the row it narrows and B12 cannot reach it, because B12's second exit passing is exactly what the narrowing removes. Unfixtured on the same missing case, and on #138.

  **Run 2 scored B9 without touching the question, and not by being careful — by luck.** It produced **no filled `0/10` anywhere**: the three filled severities landed at `7/10 itching` on case 3, `4/10` on case 6 and `6/10` on case 9. Case 3, the case #59 then said the answer turned on, was scored 7/10. The set's two `0/10`s are cases 2 and 12, and both are **givens** — the shorthand writes `no pain` — which the question does not reach. So `B9 PASS` holds under either reading and is evidence for neither. **It is still evidence for neither now that #59 is ruled**: this run tested nothing the ruling decides, and its verdict would read the same had the ruling gone the other way.

  **Run 3 did the same thing on the same three cases** — `8/10 itching` on case 3, `4/10` on case 6, `6/10` on case 9, and again no filled `0/10` anywhere. Two runs have now scored B9 without the question arising, which says the branch is unlikely to be reached by running the set again. **#59 needed a ruling rather than another run, and on 2026-08-15 it got one** — which is this prediction holding rather than a coincidence: no third run would have reached the branch either.
- **B9 leaves the silent withholding untested outside the three lung cases.** A run that declines a workup and cites nothing has no value for B9 to look up. D2, D3 and D7 cover that for cases 1, 11 and 9, because a dropped finding fails on the DRIFT side. The other **seven of B9's ten** have no such backstop — a run could quietly not act and clear both halves. (This read *six of nine* until run 2 recounted it: the row gained case 3 when `daacb11` fixed it for dropping her, and this sentence was not updated with the case list above it.) Narrower than the hole [#27](https://github.com/mshamblin5150-code/clinical-skills/issues/27) opened, and not closed.
- **B12 and B13 do not close [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67), and a run can pass both while committing it.** The visible defect is **clustering** — four filled heights at one value, six pressures on one side of a line — and neither row asks about clustering. B12 asks for a disclosure and is satisfied by nine notes writing the same honest sentence about the same `5'10"`. B13 asks about *identical* pairs and is satisfied by nine heights at one value with nine different weights. **Run 1 is the worked example: it exits zero on B13 while carrying the pattern in full.**

  **That is a limit and not an oversight, and the reason is that no threshold exists to write down.** The corpus gives a direction — about half of his transcribed pressures below 130/80 — and turning a direction into a bar over nine samples means inventing a number, which is what R5 is counted instead of enforced for. **Closing it needs a ruling from the clinician about what rate is wrong**, not another row and not another run. [#97](https://github.com/mshamblin5150-code/clinical-skills/issues/97).

  **What did close is the concealment.** Before #67 a run could choose from a template and the note said nothing; now the line has to name an anchor or admit there is none, and nine identical admissions are a visible thing in a way nine identical numbers were not. **A defect made legible is not a defect fixed**, and this bullet is here so the strike-through above is not read as one.
- **Over-declaring an attribution is unfixtured, and B11 is scoped so it cannot catch it.** `clinical-note` says an attribution whose onset line named its own symptom gets no `FILLED·asserted` line, because declaring it is reporting the skill's own compliance. B11 sits on case 9 alone, so a run that declares case 8's earache attribution as well passes every row here. The rule is stated; nothing scores it.
- **B10 and B11 were scored for the first time on run 3, and both pass.** Case 8 writes `2 days for the headache, fever, cough, sore throat and weakness, 1 day for the right earache` and case 9 writes `2 days for congestion, sneezing and cough, 1 day for the facial pain, worsening` — duration-first on both, each duration on the symptoms it was written beside. B11's declaration names both halves of the value. **Neither note raises a FLAG**, which is the reversal [#33](https://github.com/mshamblin5150-code/clinical-skills/issues/33) asked for: run 1 carried both durations and flagged them unreconciled. **Drift row 16 has now been exercised against a note** — its attribution limb, not its span limb, which has no anchor here and is fixtured as [duration-span](../duration-span/assertions.md) since [#65](https://github.com/mshamblin5150-code/clinical-skills/issues/65).

  One run is not a fix, on the symmetry this file keeps for B9: what run 3 establishes is that the FLAG is not unconditional.
- **A diabetic was given a steroid burst with no glucose comment** — case 10, methylprednisolone 125 mg IM plus a dose pack, `DM2 E11.9` coded in the same note. Case 5 has the shape too: diabetes carried into the HPI and absent from the Assessment. It is a plan-safety defect rather than a finding abandoned, so it does not fit the DRIFT shape, and a row turning on whether a caution was *worded* would belong in REPORTED — which this set does not define. Left out deliberately.

### What run 2 found that no row here catches

Four when this section was written, and the first two are `clinical-note` behaviors while the last two are defects in this file. **Both `clinical-note` behaviors are now closed** — the first became G1 and the `GIVEN` class, the second became B12, B13 and R5 — and each is struck through in place rather than deleted, because what a run found is a record and the fix is the next line of it. **Two remain open, and both are defects in this file rather than in the skill**, which is the pattern worth noticing: the rows this section wrote were fixed inside a day and the rows it criticized are still being argued about.

- ~~**A given order was dropped and overruled** — case 12.~~ **Closed: it is G1 now, and a class.** The shorthand's plan orders `spot mono`; the note carries the other six orders, omits it, and the Assessment writes `No monospot sent on those findings`. A given passing through unchanged is the first thing the tier system says, and no row here reached an **order** that was reversed: DRIFT rows are named findings, B4 is vitals, and D6 runs the other way by asking for a *generated* swab. `clinical-note` gained *A given order is a given* and **drift row 18**; this file gained the `GIVEN` section above. The row is unscored, for the reason its section states. [#66](https://github.com/mshamblin5150-code/clinical-skills/issues/66).
- ~~**The nine filled vital sets read as template-drawn.**~~ **Closed in part: they are B12, B13 and R5 now, and one of the three is a shape of row this set did not have. What is not closed has its own bullet under *Still unresolved*, and it is the larger half.** Every filled male height is `5'10"` and every filled female height is `5'4"` or `5'5"`; `138/84` appears twice and `138/86` twice; **cases 1 and 5 share an identical `5'10" / 190 lb` at ages 36 and 68.** B1 forces the values to exist, B3 forces the abnormal ones to be worked up, and nothing here scored which value was chosen — which is the license's actual instruction. It is invisible in any single note and only appears across a set, which is why twelve independent passes were needed to see it. **The per-note half became B12** — the block names what the value was reasoned from — and `clinical-note` gained *Which value was chosen is the instruction* and **drift row 19** with it. **The cross-note half became B13**, the first row here whose `Cases` column reads *the set* rather than a case list, and **R5** carries the distribution the set can count and cannot grade. All three are unscored, for the reason this section's other closures state. [#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67).
- **B9's first sentence is broader than B9's method**, and two clauses in this run sit in the gap: case 6's `and the patient is afebrile` on a filled `T 98.4 F`, and case 10's `no hypoxia` on a filled `SpO2 97%`. Both are inside a differential ranking rather than a decision to withhold, and both notes order the test anyway, so the row's own *take each decision not to act* method reaches neither. Under *every reassurance traces to a given*, both fail and this run scores `FILLED 8/9`. **The recorded 9/9 is under the row as operationalized**, and [#69](https://github.com/mshamblin5150-code/clinical-skills/issues/69) is what settles which. Case 6's clause also contradicts its own row 15 verdict, which claims no decision rests on `98.4 F`.
- **C1 never says what an entry is.** Case 11 argues down herpes simplex, primary syphilitic chancre and molluscum contagiosum in a prose paragraph carrying no code and saying so. Read as prose it is 12 entries and 12 codes and C1 passes; read as three entries it is 15 and 12 and **C1 fails, making this run `CODING 1/2`**. It was scored as a refusal, because all 114 entries in the set follow one `Name - CODE: rationale` form and this follows none of it — but that makes the row evadable by formatting, which is the opposite of what *two counts that either match or do not* promises. [#70](https://github.com/mshamblin5150-code/clinical-skills/issues/70), with [#68](https://github.com/mshamblin5150-code/clinical-skills/issues/68) as the same seam from the skill's side.

**Two of these four could change a recorded score**, and neither is an agent's to decide. The scorecard at the top of this file states what it states under the rows as they are written today, and names the two tickets that could move it.

### What run 3 found that no row here catches

Four again, and three of them are the run-2 findings recurring rather than new ones — which is itself the finding.

- **[#67](https://github.com/mshamblin5150-code/clinical-skills/issues/67) is no longer an inference from one run's spread.** Run 2 saw the template in repeated values *within* a set. Run 3 sees it **across** sets: case 8's `148/92` is run 1's value for that case and case 9's `138/86` is run 2's, reproduced by passes that could see neither. Every filled male height is `5'10"` for a third time. **Nothing here scores which value was chosen**, which is the license's actual instruction, and no single note can show it.
- **[#69](https://github.com/mshamblin5150-code/clinical-skills/issues/69)'s gap held one clause this run**, down from two. Case 9's pneumonia entry argues down on `the absence of crackles, egophony, dyspnea, pleuritic pain and hypoxia`, and the `hypoxia` limb is readable only off a `SpO2 96%` the block declares filled. It sits inside a differential ranking, the film is ordered anyway, and the note states the SpO2 *"was not allowed to defer the chest radiograph"* — so B9's *take each decision not to act* method does not reach it. Under *every reassurance traces to a given* it fails and this run scores **`FILLED 9/11`**. The recorded 10/11 is under the row as operationalized, and #69 is exactly as open as it was.
- **[#70](https://github.com/mshamblin5150-code/clinical-skills/issues/70)'s seam widened, and it moved.** Run 2's ambiguity was one note arguing diagnoses down in an uncoded prose paragraph *inside* the differential; that shape does not recur, and case 11 is now a clean ten-entry coded list. What all twelve notes do instead is place a **second, separately headed block after the differential** — `Additional problems addressed today`, `Also addressed this visit`, `Reasoning carried forward` — and in four of them the items are diagnosis-shaped and mixed, coded and uncoded side by side. Folded in, case 2 reads 9/8, case 8 reads 14/12, case 9 reads 13/12, case 10 reads 14/8, and **C1 fails**. It was scored on the narrow reading — the block the note itself labels `Differential:` — because these sit after `Final diagnosis:`, are labeled as problems *addressed* rather than as ranked candidates, and appear in all twelve rather than being one note's escape. **The row still does not say what an entry is, and now it does not say where the differential ends either.** Both questions are on #70, and **settling the first does not settle the second** — an entry definition leaves `Also addressed this visit` exactly as ambiguous as it was.
- **A `NKDA` was filled over a documented allergy**, and this one is new. Case 2's shorthand lists `seasonal allergies` in its `hx:` line; the note routes it to PMH, codes `J30.2` and prescribes cetirizine — so nothing is lost — and then writes `Allergies (reaction): NKDA` and declares `ALLERGIES NKDA filled`. The filled-value license requires that *the shorthand constrains none*, and here it arguably does. **Case 11 is the counter-example in the same run**: its shorthand labels the same words `allergies:` and it puts them in the box. R1 cannot catch this — #29 struck its allergy half — and R2 cannot, because `NKDA` is a value and not a hedge. Whether an allergy slot may read `NKDA` beside a documented environmental allergy is a clinical question, and it is the clinician's.

**Only the second and third could change a recorded score**, and neither is an agent's to decide. The scorecard states what it states under the rows as written today.
