# Index-and-table descriptor-agreement control

This is issue #1310's retained blind control for ADR 0248 ruling 10. The two
short notes preserve the relevant, de-identified words from the committed real
notes named below; they remove unrelated sections so the public bidirectional
bind has one exact population to grade.

- `injury.md` is derived from `fixtures/filled-anchor/notes/case-06.md`.
- `neoplasm.md` is derived from `fixtures/blind-run/obesity-bmi-case-01.md`.

A separate reader received only `anchor_scan.py --agreement-brief` output and
wrote `agreement-read.json`; the reader did not open either worksheet. A
non-authoring review grades the record through `--agreement-read`. The tests
also plant left-thumb, sequela, uncertain-behavior, and malignant-code
mutations after copying this clean control. These files are fixtures, not
patient records.
