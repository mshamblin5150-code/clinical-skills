# Threshold draft record fixture

This directory preserves the recommendation-record shape used by
`tools/threshold_draft.py`'s refusal scan. The JSON was reduced from a working
recommendation record: recommendation text and entries were removed, and the source
path names the synthetic catalog document used by the test. It contains no patient
material and no guideline expression.
