# Authored-anchor descriptor-agreement control

The note and worksheet were generated from `input.md`, a neutral de-identified
case with no visit date or site. Every entry, differential, and refusal row has
an authored, contiguous note quotation. A separate reader received only the
answer-free brief, retried after the first grader's finding categories, and
produced `agreement-read.json`. A fresh non-authoring check of that revision
exited 0. The rejected first record and both graders' reports were run-private,
not added to this control.
